# Screenshot Manifest

PNG files in this folder were rendered from the listed pages with:

```bash
qlmanage -t -s 1280 -o .artifacts/screenshots <page>
```

## 01-broken-coupon-ui.png

- What it shows: Checkout UI showing SUMMER-2026 accepted by validation and rejected later by generic checkout error.
- Command/page used to generate it: .artifacts/screenshots/pages/01-broken-coupon-ui.html; browser equivalent is npm run dev at http://localhost:4173.
- Why it belongs in the post: This is the user-visible bug the forks investigate.

## 02-failing-tests.png

- What it shows: Baseline tests defining the desired inline invalid coupon behavior.
- Command/page used to generate it: .artifacts/screenshots/pages/02-failing-tests.html from .artifacts/logs/failing-baseline.txt.
- Why it belongs in the post: Shows useful failing state collected before forking.

## 03-debug-brief.png

- What it shows: The debug brief written into the prepared parent sandbox.
- Command/page used to generate it: .artifacts/screenshots/pages/03-debug-brief.html from debug-brief.md.
- Why it belongs in the post: Shows agent/debugging state externalized to disk.

## 04-inspect-state.png

- What it shows: Prepared parent state checklist.
- Command/page used to generate it: .artifacts/screenshots/pages/04-inspect-state.html from npm run inspect-state.
- Why it belongs in the post: Proves the parent has reusable work before fork.

## 05-lifecycle-demo.png

- What it shows: Lifecycle semantics terminal output.
- Command/page used to generate it: .artifacts/screenshots/pages/05-lifecycle-demo.html from npm run lifecycle-demo.
- Why it belongs in the post: Supports the guide's distinction between stop, pause, fork, snapshot, and restore.

## 06-fork-tree.png

- What it shows: Multi-level fork tree output with hypothesis branches, candidate fix state, validators, memory probe, and fork-a-fork refinements.
- Command/page used to generate it: .artifacts/screenshots/pages/06-fork-tree.html from npm run fork-tree-demo.
- Why it belongs in the post: Explains why the useful fork point is the changed contract branch, not only the original baseline.

## 07-state-reuse-benchmark.png

- What it shows: Benchmark-style bar chart comparing fresh setup, stop/start, fork, and snapshot restore.
- Command/page used to generate it: .artifacts/screenshots/pages/07-state-reuse-benchmark.html using docs/graphs/state-reuse-benchmark.svg.
- Why it belongs in the post: Visualizes repeated work avoided by prepared state reuse.

## 08-branch-outcomes.png

- What it shows: Matrix-style visual comparing frontend, backend, and contract hypotheses.
- Command/page used to generate it: .artifacts/screenshots/pages/08-branch-outcomes.html using docs/graphs/branch-outcomes.svg.
- Why it belongs in the post: Shows why the contract hypothesis is the best branch.

## 10-video-terminal-waiting.png

- What it shows: The static reset output showing exactly where the browser coupon bug appears.
- Command/page used to generate it: .artifacts/screenshots/pages/10-video-terminal-waiting.html, representing npm run demo:reset.
- Why it belongs in the post: Shows the browser-to-terminal handoff without relying on a moving terminal.

## 11-video-terminal-complete.png

- What it shows: The command-driven multi-branch fork run: prepared parent, first fork point, contract fork-a-fork, and validators.
- Command/page used to generate it: .artifacts/screenshots/pages/11-video-terminal-complete.html from .artifacts/logs/simple-demo-run.json.
- Why it belongs in the post: Shows the simpler static terminal story as the main video artifact.

## 12-video-latency-report.png

- What it shows: A static command latency report for the fork stages.
- Command/page used to generate it: .artifacts/screenshots/pages/12-video-latency-report.html from .artifacts/logs/simple-demo-run.json.
- Why it belongs in the post: Gives the guide a concrete latency artifact without requiring an animated terminal.

## 13-agent-reasoning.png

- What it shows: Live OpenAI-generated externalized debugging rationale for frontend, backend, and contract hypothesis forks.
- Command/page used to generate it: .artifacts/screenshots/pages/13-agent-reasoning.html from .artifacts/logs/agent-reasoning.md, generated by npm run demo:agent.
- Why it belongs in the post: Shows the actual agent reasoning used to explain why the contract fork is the best next parent.

## 14-guide-live-fork-run.png

- What it shows: The latest live Daytona terminal run with measured parent creation, prepared-state capture, first-level hypothesis forks, and the explanatory break after the parent is prepared.
- Command/page used to generate it: .artifacts/screenshots/pages/14-guide-live-fork-run.html, generated by `node scripts/render-guide-assets.js` from .artifacts/logs/daytona-live-run.txt, then rendered with `qlmanage -t -s 1280`.
- Why it belongs in the post: Shows the demo operating on real live sandbox operations instead of only a conceptual fork tree.

## 15-guide-live-latency.png

- What it shows: A compact timing table from the latest successful live run: parent create, prepared state, five fork operations, candidate-fix writes, and fork latency total.
- Command/page used to generate it: .artifacts/screenshots/pages/15-guide-live-latency.html, generated by `node scripts/render-guide-assets.js` from .artifacts/logs/daytona-live-latency.json, then rendered with `qlmanage -t -s 1280`.
- Why it belongs in the post: Gives the guide a concrete benchmark-style visual while keeping the numbers framed as measured demo observations.

## 16-guide-current-fork-tree.png

- What it shows: Current default recording fork tree: coupon-parent-prepared branching into frontend, backend, and contract hypotheses, then contract-hypothesis branching again into contract-minimal and contract-with-regression-test.
- Command/page used to generate it: .artifacts/screenshots/pages/16-guide-current-fork-tree.html, generated by `node scripts/render-guide-assets.js`, then rendered with `qlmanage -t -s 1280`.
- Why it belongs in the post: Shows the central multi-level forking argument: the second fork happens after changed state exists in the contract branch.

## 17-guide-state-semantics.png

- What it shows: State layers and lifecycle semantics for filesystem state, sandbox configuration state, runtime/process state, agent/debugging state, stop/start, pause, fork, snapshot, and restore.
- Command/page used to generate it: .artifacts/screenshots/pages/17-guide-state-semantics.html, generated by `node scripts/render-guide-assets.js` using docs/graphs/state-layers.svg and docs/graphs/lifecycle-comparison.svg, then rendered with `qlmanage -t -s 1280`.
- Why it belongs in the post: Keeps the technical claims precise by showing that lifecycle operations preserve different kinds of state.

## guide-inserts/01-demo-bug.png

- What it shows: Daytona dark-mode styled recording UI with the reproduced broken coupon state and neutral hypothesis cards.
- Command/page used to generate it: .artifacts/screenshots/pages/guide-01-demo-bug.html, generated by `node scripts/render-guide-assets.js`, then rendered with `qlmanage -t -s 1280`.
- Why it belongs in the post: Replaces `[SCREENSHOT: broken coupon UI]` in the current guide draft.

## guide-inserts/02-prepared-parent-state.png

- What it shows: Daytona dark-mode styled prepared parent checklist with `npm run inspect-state` output and reusable state artifacts.
- Command/page used to generate it: .artifacts/screenshots/pages/guide-02-prepared-parent-state.html, generated by `node scripts/render-guide-assets.js`, then rendered with `qlmanage -t -s 1280`.
- Why it belongs in the post: Replaces `[SCREENSHOT: prepared parent state checklist]` in the current guide draft.

## guide-inserts/03-live-fork-run.png

- What it shows: Daytona dark-mode styled live Daytona terminal run with prepared parent setup, fork timings, and explanation blocks.
- Command/page used to generate it: .artifacts/screenshots/pages/guide-03-live-fork-run.html, generated by `node scripts/render-guide-assets.js` from .artifacts/logs/daytona-live-run.txt, then rendered with `qlmanage -t -s 1280`.
- Why it belongs in the post: Replaces `[SCREENSHOT: live Daytona terminal run]` in the current guide draft.

## guide-inserts/04-multilevel-fork-tree.png

- What it shows: Daytona dark-mode styled level view of the multi-level fork tree: prepared parent, hypothesis forks, and refinement forks from `contract-hypothesis`.
- Command/page used to generate it: .artifacts/screenshots/pages/guide-04-multilevel-fork-tree.html, generated by `node scripts/render-guide-assets.js`, then rendered with `qlmanage -t -s 1280`.
- Why it belongs in the post: Replaces `[SCREENSHOT: current fork tree]` in the current guide draft when an image is needed. Prefer the native Markdown version at `docs/blog-components/markdown/04-multilevel-fork-tree.md` for blog readability.

## guide-inserts/05-state-semantics.png

- What it shows: Daytona dark-mode styled state layers and lifecycle semantics visual with stop/start, fork, and snapshot cards.
- Command/page used to generate it: .artifacts/screenshots/pages/guide-05-state-semantics.html, generated by `node scripts/render-guide-assets.js` using docs/graphs/state-layers.svg and docs/graphs/lifecycle-comparison.svg, then rendered with `qlmanage -t -s 1280`.
- Why it belongs in the post: Replaces `[SCREENSHOT: state layers and lifecycle semantics]` in the current guide draft when an image is needed. Prefer the native Markdown version at `docs/blog-components/markdown/05-state-semantics.md` for blog readability.
