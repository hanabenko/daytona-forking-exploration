# Guide Image Inserts

These are the five screenshots prepared for the current guide draft. They are rendered from current-UI guide pages generated by `scripts/render-guide-assets.js`, not copied from the older static screenshot set. The current set uses a Daytona docs-style dark theme with green highlights.

All files are under `.artifacts/screenshots/guide-inserts/` so they can be inserted without referencing older working filenames.

## Insert Map

### `[SCREENSHOT: broken coupon UI]`

```md
![Checkout UI accepts the invalid SUMMER-2026 coupon, then checkout fails later with a generic error.](.artifacts/screenshots/guide-inserts/01-demo-bug.png)
```

Caption: The reproduced bug in the dark-mode demo UI: the invalid coupon is accepted before checkout fails with a generic error.

Why here: opens the guide with the visible failure that motivates the fork tree.

### `[SCREENSHOT: prepared parent state checklist]`

```md
![Prepared parent sandbox checklist showing repo files, dependencies, build artifacts, seeded data, logs, debug brief, state marker, environment, and app start checks.](.artifacts/screenshots/guide-inserts/02-prepared-parent-state.png)
```

Caption: The parent sandbox contains setup and debugging artifacts before it is forked.

Why here: shows that children inherit useful prepared state, not just a clean repository clone.

### `[SCREENSHOT: live Daytona terminal run]`

```md
![Live Daytona terminal run creating the prepared parent sandbox, preparing state, and forking the hypothesis branches with measured operation timings.](.artifacts/screenshots/guide-inserts/03-live-fork-run.png)
```

Caption: A live run creates the prepared parent, then forks independent hypothesis sandboxes from that state.

Why here: supports the live walkthrough section with actual SDK-timed sandbox operations.

### `[SCREENSHOT: current fork tree]`

Preferred Markdown insert:

```md
<!-- Paste the contents of docs/blog-components/markdown/04-multilevel-fork-tree.md here. -->
```

Markdown source: `docs/blog-components/markdown/04-multilevel-fork-tree.md`

PNG fallback:

```md
![Fork tree showing coupon-parent-prepared branching into frontend, backend, and contract hypotheses, then contract-hypothesis branching into minimal and regression-test refinements.](.artifacts/screenshots/guide-inserts/04-multilevel-fork-tree.png)
```

Caption: The second fork point starts from the changed contract branch, not from the original baseline.

Why here: makes the multi-level forking argument concrete.

### `[SCREENSHOT: state layers and lifecycle semantics]`

Preferred Markdown insert:

```md
<!-- Paste the contents of docs/blog-components/markdown/05-state-semantics.md here. -->
```

Markdown source: `docs/blog-components/markdown/05-state-semantics.md`

PNG fallback:

```md
![State layers and lifecycle comparison showing filesystem state, sandbox configuration, runtime process state, externalized agent state, and how stop/start, pause, fork, snapshot, and restore differ.](.artifacts/screenshots/guide-inserts/05-state-semantics.png)
```

Caption: Different lifecycle operations preserve different kinds of state.

Why here: anchors the semantics section and keeps the guide from collapsing stop/start, pause, fork, snapshot, and restore into one idea.

## Optional Swap

If the guide needs a concrete timing visual instead of the lifecycle semantics screenshot, use:

```md
![Measured live demo timings for parent creation, prepared state, fork operations, and candidate fix writes.](.artifacts/screenshots/15-guide-live-latency.png)
```

Caption: Measured timings from the latest live demo run. Treat these as demo observations, not general Daytona performance claims.
