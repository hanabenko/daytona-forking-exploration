# Work Summary

## Milestone 1: Initial scaffold

- What changed: created the contained `daytona-forking-coupon-demo` project structure with a small checkout app, seeded coupon data, a deliberate shared validation bug, baseline state files, and failing tests that describe the desired behavior.
- Files created/modified: `package.json`, `src/shared/couponContract.js`, `src/server/app.js`, `src/server/index.js`, `src/client/index.html`, `src/client/styles.css`, `src/client/app.js`, `data/coupons.json`, `state-marker.txt`, `state-manifest.json`, `debug-brief.md`, `tests/ui.test.js`, `tests/api.test.js`, `tests/e2e.test.js`.
- Commands run: `mkdir -p ...` to create the subproject folders.
- Test results: not run yet.
- Screenshots captured: none yet.
- Remaining TODOs: add lifecycle scripts, graph generation, docs, benchmark output, screenshot manifest, run install/build/seed/tests, capture screenshots if feasible.
- Assumptions/blockers: the demo is intentionally dependency-light and keeps the baseline bug in place so the parent sandbox has useful failing state before forking.

## Milestone 2: Lifecycle, branch, graph, and screenshot scripts

- What changed: added deterministic scripts for seeding, building, inspecting prepared state, printing lifecycle semantics, printing the fork tree, comparing hypothesis branches, generating benchmark data, rendering SVG graphs, and generating screenshot pages plus a manifest.
- Files created/modified: `scripts/state-utils.js`, `scripts/seed.js`, `scripts/build.js`, `scripts/inspect-state.js`, `scripts/lifecycle-demo.js`, `scripts/fork-tree-demo.js`, `scripts/compare-branches.js`, `scripts/benchmark-state-reuse.js`, `scripts/render-graphs.js`, `scripts/capture-screenshots.js`, `SUMMARY.md`.
- Commands run: no validation commands yet; files were written via patch.
- Test results: not run yet.
- Screenshots captured: none yet; screenshot HTML page generation is scripted.
- Remaining TODOs: add README/DEMO/semantics docs, add Daytona TypeScript examples, run install/build/seed/tests/benchmarks/graphs, capture PNG screenshots if feasible.
- Assumptions/blockers: `capture-screenshots` uses Playwright when installed and otherwise creates screenshot-ready HTML pages for browser capture.

## Milestone 3: Guide docs and Daytona examples

- What changed: added the developer guide entry points, required lifecycle comparison table, state-layer explanation, screenshot checklist, benchmark JSON seed data, and TypeScript Daytona lifecycle examples.
- Files created/modified: `README.md`, `DEMO.md`, `.env`, `docs/forking-semantics.md`, `docs/screenshot-checklist.md`, `docs/graphs/state-reuse-benchmark.json`, `daytona-examples/stop-start-persistence.ts`, `daytona-examples/fork-independence.ts`, `daytona-examples/fork-a-fork.ts`, `daytona-examples/snapshot-vs-fork.ts`, `daytona-examples/pause-memory-state.ts`, `daytona-examples/create-from-snapshot.ts`, `SUMMARY.md`.
- Commands run: checked current Daytona docs by browser lookup for TypeScript create, fork, snapshot, start, and stop examples.
- Test results: not run yet.
- Screenshots captured: none yet.
- Remaining TODOs: run `npm install`, seed/build/test, save failing baseline logs, generate graphs, generate screenshot manifest/pages, capture PNG screenshots if feasible.
- Assumptions/blockers: Daytona pause/resume support is represented as runner/API-dependent in the example instead of pretending ordinary stop/start is pause.

## Milestone 4: Validation, screenshots, and run notes

- What changed: installed npm metadata, seeded data, built artifacts, saved failing baseline test logs, generated benchmark JSON and SVG graphs, generated screenshot pages/manifest, rendered the requested PNG screenshots, and updated root `runs/` to point at this coupon demo as the current run.
- Files created/modified: `package-lock.json`, `dist/`, `logs/failing-baseline.txt`, `docs/graphs/*.svg`, `docs/graphs/index.html`, `docs/graphs/state-reuse-benchmark.json`, `docs/screenshots/pages/*.html`, `docs/screenshots/*.png`, `docs/screenshots/manifest.md`, `state-manifest.json`, `tests/e2e.test.js`, `scripts/capture-screenshots.js`, `../runs/README.md`, `../runs/coupon-demo-20260626-114414.md`, `SUMMARY.md`.
- Commands run: `npm install`, `npm run seed`, `npm run build`, `npm test > logs/failing-baseline.txt 2>&1`, `npm run benchmark-state-reuse`, `npm run render-graphs`, `npm run inspect-state`, `npm run lifecycle-demo`, `npm run fork-tree-demo`, `npm run compare-branches`, `npm run capture-screenshots`, `qlmanage -t -s 1280 -o docs/screenshots ...`, `npm run test:ui`, `npm run test:api`, `npm run test:e2e`.
- Test results: `npm run inspect-state`, `lifecycle-demo`, `fork-tree-demo`, and `compare-branches` passed. `npm test`, `test:ui`, `test:api`, and `test:e2e` fail intentionally on the baseline coupon contract bug, with failing output saved in `logs/failing-baseline.txt`.
- Screenshots captured: `docs/screenshots/01-broken-coupon-ui.png`, `02-failing-tests.png`, `03-debug-brief.png`, `04-inspect-state.png`, `05-lifecycle-demo.png`, `06-fork-tree.png`, `07-state-reuse-benchmark.png`, `08-branch-outcomes.png`.
- Remaining TODOs: execute the Daytona lifecycle examples against real Daytona credentials/sandboxes if a live proof is needed; optionally replace simulated benchmark values with live Daytona timings.
- Assumptions/blockers: local port binding required approval for the dev server; the in-app browser endpoint was unavailable, so screenshots were rendered from deterministic HTML pages with Quick Look.

## Milestone 5: Visual forking console and live Daytona key path

- What changed: added `/forking-demo`, a visual forking console that shows the prepared parent, hypothesis forks, fork-a-fork refinements, and a post-fix validator fan-out. Added `.env.daytona.example`, local key loading via `.env.daytona.local`, `npm run daytona:check`, `npm run daytona:plan`, and `npm run daytona:live`.
- Files created/modified: `.env.daytona.example`, `.gitignore`, `package.json`, `package-lock.json`, `data/forking-console-state.json`, `src/server/app.js`, `src/client/index.html`, `src/client/forking-demo.html`, `src/client/forking-demo.css`, `src/client/forking-demo.js`, `scripts/daytona-live-demo.js`, `scripts/validator-fanout-demo.js`, `README.md`, `DEMO.md`, `docs/forking-semantics.md`, `docs/screenshot-checklist.md`, `../runs/coupon-demo-20260626-114414.md`, `SUMMARY.md`.
- Commands run: `npm view @daytona/sdk version`, `npm install`, `npm run daytona:check`, `npm run daytona:plan`, `npm run validator-fanout-demo`, `npm run build`, restarted `npm run dev`, `curl http://localhost:4173/forking-demo`, `curl http://localhost:4173/api/forking-state`.
- Test results: `daytona:check`, `daytona:plan`, `validator-fanout-demo`, and `build` passed. The live Daytona run was not executed because no `DAYTONA_API_KEY` was configured.
- Screenshots captured: no new PNG; the existing screenshot set remains valid. Attempted `qlmanage` against the live `/forking-demo` URL, but Quick Look hung on the live URL and was stopped.
- Remaining TODOs: run `npm run daytona:live` with a real Daytona API key; capture a browser screenshot of `/forking-demo` using Playwright or the in-app browser if available.
- Assumptions/blockers: the browser UI intentionally never asks for the API key; credentials stay in shell environment or untracked `.env.daytona.local`. `npm install` reported 27 vulnerabilities in the installed dependency tree after adding `@daytona/sdk`; no audit remediation was applied.

## Milestone 6: Browser-started live run, checking agent, and memory probe

- What changed: changed `/forking-demo` so it asks for the Daytona API key once after the user reproduces the bug, then starts the live run through the local server. Added clear Fork Spawn Log and Live Daytona Run Log panels, a fourth `checking-agent` validator, and a conservative `memory-state-check` probe that warms a process before forking and logs whether the child observes live memory state.
- Files created/modified: `src/server/app.js`, `src/client/app.js`, `src/client/forking-demo.html`, `src/client/forking-demo.css`, `src/client/forking-demo.js`, `data/forking-console-state.json`, `scripts/daytona-live-demo.js`, `scripts/validator-fanout-demo.js`, `README.md`, `DEMO.md`, `docs/forking-semantics.md`, `docs/screenshot-checklist.md`, `../runs/coupon-demo-20260626-114414.md`, `SUMMARY.md`.
- Commands run: `node --check scripts/daytona-live-demo.js`, `node --check src/client/forking-demo.js`, `node --check src/server/app.js`, JSON parse check for `data/forking-console-state.json`, `npm run validator-fanout-demo`, `npm run daytona:check`, `npm run daytona:plan`, `npm run build`, restarted `npm run dev`, `curl http://localhost:4173/forking-demo`, `curl http://localhost:4173/api/forking-state`, `curl -X POST http://localhost:4173/api/daytona-run -d '{}'`.
- Test results: syntax checks passed; `validator-fanout-demo`, `daytona:check`, `daytona:plan`, and `build` passed. The run endpoint correctly rejects missing API keys. Full `npm test` was not rerun because the baseline is intentionally failing.
- Screenshots captured: no new screenshots.
- Remaining TODOs: run the browser-started live Daytona flow with a real key; capture `/forking-demo` after a live run; decide whether to keep or clean up live sandboxes after recording.
- Assumptions/blockers: the API key is posted only to the local server and passed to the child process as `DAYTONA_API_KEY`; it is not written to files or logs. The memory probe is deliberately observational and must not be described as guaranteed runtime-memory preservation.

## Milestone 7: Step 1 always-on root console

- What changed: refactored `/` into the always-on demo console. The root page now shows checkout bug reproduction, Daytona key/start controls, fork tree, prepared-state checklist, lifecycle steps, clone-vs-fork comparison, validator fan-out, memory probe, fork spawn log, evidence panels, and live run log. `/forking-demo` remains available as a console-only fallback.
- Files created/modified: `src/client/index.html`, `src/client/styles.css`, `README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `../runs/coupon-demo-20260626-114414.md`, `SUMMARY.md`.
- Commands run: `npm run build`, `node --check src/client/app.js`, `node --check src/client/forking-demo.js`, `rg ... src/client/index.html`, `curl http://localhost:4173/`, `curl http://localhost:4173/api/forking-state`.
- Test results: build and client syntax checks passed. Running server serves the updated root page and state endpoint.
- Screenshots captured: no new screenshots.
- Remaining TODOs: step 2 should replace the browser-only reproduction gate with server-side reproduction tracking in `data/demo-session-state.json`.
- Assumptions/blockers: this step intentionally kept the existing browser-side reproduction gate until step 2; the UI is now always visible.

## Milestone 8: Step 2 server-side reproduction tracking

- What changed: replaced the browser-only reproduction gate with durable server-side demo session state. The checkout flow now posts `POST /api/reproduction-event` after `SUMMER-2026` is accepted and checkout fails generically. The server writes `data/demo-session-state.json`, exposes `GET /api/demo-session`, supports `POST /api/demo-session/reset`, and blocks `POST /api/daytona-run` until `bugReproduced` is true.
- Files created/modified: `data/demo-session-state.json`, `src/server/app.js`, `src/client/app.js`, `src/client/forking-demo.js`, `README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `../runs/coupon-demo-20260626-114414.md`, `SUMMARY.md`.
- Commands run: `node --check src/server/app.js`, `node --check src/client/app.js`, `node --check src/client/forking-demo.js`, `npm run build`, JSON parse check for `data/demo-session-state.json`, restarted `npm run dev`, `curl -X POST /api/demo-session/reset`, `curl /api/demo-session`, `curl -X POST /api/daytona-run` before reproduction, `curl -X POST /api/reproduction-event`, read `data/demo-session-state.json`, reset session state again.
- Test results: syntax checks and build passed. The live-run endpoint correctly rejects an API key before reproduction. The reproduction endpoint persists `bugReproduced: true` for the exact `SUMMER-2026` accepted-then-generic-failure event.
- Screenshots captured: no new screenshots.
- Remaining TODOs: step 3 can refine the browser UI around the server-side state, including a visible reset button and clearer "Fork reproduced state" affordance.
- Assumptions/blockers: no real Daytona run was started during verification; a fake key was only used to verify the pre-reproduction rejection path.

## Milestone 9: Step 3 visible reproduced-state controls and checklist

- What changed: added a visible server-state panel, `Reset Reproduction` button, dynamic `Fork reproduced state` button state, and a `Demo Step Checklist` panel that shows all demo steps and their current status. This makes the server-side reproduction gate visible in the UI instead of hidden behind form disabled state.
- Files created/modified: `src/client/index.html`, `src/client/forking-demo.html`, `src/client/forking-demo.css`, `src/client/forking-demo.js`, `README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `../runs/coupon-demo-20260626-114414.md`, `SUMMARY.md`.
- Commands run: `node --check src/client/forking-demo.js`, `node --check src/client/app.js`, `node --check src/server/app.js`, `npm run build`, `rg ...`, restarted `npm run dev`, `curl http://localhost:4173/`, `curl -X POST http://localhost:4173/api/demo-session/reset`, `curl http://localhost:4173/api/demo-session`, `curl http://localhost:4173/api/daytona-run`.
- Test results: syntax checks and build passed. The running root page includes the checklist, reset button, and fork reproduced state control. The session reset endpoint returns a clean unreproduced state.
- Screenshots captured: no new screenshots.
- Remaining TODOs: step 4 should make the Fork Spawn Log fully structured from live run events, including timestamps, inherited state, action, result, and sandbox IDs.
- Assumptions/blockers: no real Daytona run was started; local server is running at `http://localhost:4173`.

## Milestone 10: Step 4 structured fork spawn log

- What changed: upgraded `forkEvents` from short text rows to structured timeline records with `at`, `fork`, `from`, `sandboxId`, `parentSandboxId`, `inheritedState`, `action`, `result`, and `status`. Updated both simulated state and the live Daytona runner to produce the same schema. Updated the UI to render timestamps, sandbox IDs, inherited-state chips, action, result, and status.
- Files created/modified: `data/forking-console-state.json`, `scripts/daytona-live-demo.js`, `src/client/forking-demo.js`, `src/client/forking-demo.css`, `src/client/index.html`, `src/client/forking-demo.html`, `README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `../runs/coupon-demo-20260626-114414.md`, `SUMMARY.md`.
- Commands run: `node --check scripts/daytona-live-demo.js`, `node --check src/client/forking-demo.js`, `node --check src/server/app.js`, JSON parse check for `data/forking-console-state.json`, `npm run build`, `curl http://localhost:4173/api/forking-state`, `curl http://localhost:4173/`.
- Test results: syntax checks and build passed. The state endpoint returns 11 structured fork events. The running root page shows the updated Fork Spawn Log copy.
- Screenshots captured: no new screenshots.
- Remaining TODOs: step 5 should deepen the multi-level fork tree UI and live event detail so it clearly demonstrates why forking beats three concurrent clones.
- Assumptions/blockers: no real Daytona run was started; live runner changes are syntax-checked but not exercised against Daytona credentials.

## Milestone 11: Step 5 multi-level fork tree

- What changed: added a first-class `forkTreeLevels` data model and a data-driven clone-vs-fork comparison. The root console and `/forking-demo` now show a full multi-level lineage: prepared parent, hypothesis forks, candidate contract-fix state, fork-a-fork refinements, validator fan-out, checking agent, and memory-state probe. Updated the live Daytona runner so real sandbox IDs can populate the multi-level tree as forks are spawned.
- Files created/modified: `data/forking-console-state.json`, `src/client/index.html`, `src/client/forking-demo.html`, `src/client/forking-demo.js`, `src/client/forking-demo.css`, `scripts/daytona-live-demo.js`, `scripts/fork-tree-demo.js`, `scripts/render-graphs.js`, `scripts/capture-screenshots.js`, `docs/graphs/fork-tree.svg`, `docs/graphs/index.html`, `docs/screenshots/pages/06-fork-tree.html`, `docs/screenshots/06-fork-tree.png`, `docs/screenshots/manifest.md`, `docs/screenshot-checklist.md`, `README.md`, `DEMO.md`, `../runs/coupon-demo-20260626-114414.md`, `SUMMARY.md`.
- Commands run: `node --check src/client/forking-demo.js`, `node --check scripts/daytona-live-demo.js`, `node --check scripts/render-graphs.js`, JSON parse checks for `data/forking-console-state.json`, `npm run build`, `npm run fork-tree-demo`, `npm run daytona:plan`, `npm run render-graphs`, `npm run capture-screenshots`, `qlmanage -t -s 1280 -o /private/tmp/daytona-forking-step5-screens docs/screenshots/pages/06-fork-tree.html`, `cp /private/tmp/daytona-forking-step5-screens/06-fork-tree.html.png docs/screenshots/06-fork-tree.png`, `curl http://localhost:4173/api/forking-state`, `curl http://localhost:4173/`, `curl http://localhost:4173/forking-demo`, `file docs/screenshots/06-fork-tree.png docs/graphs/fork-tree.svg`, `rg ... contract-candidate-fix ...`, `tail -n 60 SUMMARY.md`.
- Test results: syntax checks passed; JSON validation confirmed 5 fork-tree levels; `npm run build`, `npm run fork-tree-demo`, `npm run daytona:plan`, `npm run render-graphs`, and `npm run capture-screenshots` passed. The state endpoint serves the new `forkTreeLevels` and `cloneComparison` data.
- Screenshots captured: refreshed `docs/screenshots/06-fork-tree.png` from the regenerated `docs/screenshots/pages/06-fork-tree.html`; visually checked the PNG.
- Remaining TODOs: run the live Daytona flow with a real API key, capture the full browser console after live sandbox IDs populate, and decide whether to keep or clean up live sandboxes.
- Assumptions/blockers: no real Daytona run was started. Playwright is not installed, so the screenshot script regenerated HTML pages and manifest only; Quick Look was used to refresh the affected PNG.

## Milestone 12: Step 6 checking agent

- What changed: made the checking agent a first-class demo element instead of only a validator card. Added durable checker artifacts, a deterministic `npm run checking-agent-demo` command, a `checkingAgent` state object with inputs/gates/decision/fallback route, a dedicated Checking Agent Decision panel on `/` and `/forking-demo`, and live-run updates so a real Daytona checking fork can populate the same panel.
- Files created/modified: `validator-summary.json`, `checking-agent-report.md`, `scripts/checking-agent-demo.js`, `package.json`, `data/forking-console-state.json`, `src/client/index.html`, `src/client/forking-demo.html`, `src/client/forking-demo.js`, `src/client/forking-demo.css`, `scripts/daytona-live-demo.js`, `scripts/capture-screenshots.js`, `docs/screenshots/pages/09-checking-agent.html`, `docs/screenshots/09-checking-agent.png`, `docs/screenshots/manifest.md`, `docs/screenshot-checklist.md`, `README.md`, `DEMO.md`, `../runs/coupon-demo-20260626-114414.md`, `SUMMARY.md`.
- Commands run: `node --check src/client/forking-demo.js`, `node --check scripts/daytona-live-demo.js`, `node --check scripts/checking-agent-demo.js`, `node --check scripts/capture-screenshots.js`, JSON parse checks for `data/forking-console-state.json` and `validator-summary.json`, `npm run checking-agent-demo`, `npm run build`, `npm run daytona:plan`, `npm run capture-screenshots`, `qlmanage -t -s 1280 -o /private/tmp/daytona-forking-step6-screens docs/screenshots/pages/09-checking-agent.html`, `cp /private/tmp/daytona-forking-step6-screens/09-checking-agent.html.png docs/screenshots/09-checking-agent.png`, `curl http://localhost:4173/api/forking-state`, `curl http://localhost:4173/`, `rg ... checking-agent ...`, `file docs/screenshots/09-checking-agent.png`.
- Test results: syntax checks passed; JSON validation passed; `npm run checking-agent-demo`, `npm run build`, `npm run daytona:plan`, and `npm run capture-screenshots` passed. The state endpoint now serves `checkingAgent`, and the root page includes the Checking Agent Decision panel.
- Screenshots captured: added `docs/screenshots/09-checking-agent.png` from `docs/screenshots/pages/09-checking-agent.html`; visually checked the PNG.
- Remaining TODOs: run the live Daytona flow with a real API key to populate the checking-agent sandbox ID from Daytona, then capture the full browser console with live IDs.
- Assumptions/blockers: no real Daytona run was started. Playwright is not installed, so the screenshot script generated HTML and manifest only; Quick Look was used to render the new PNG.

## Current Step Checklist

1. Always-on console at `/`: complete.
2. Server-side reproduction tracking: complete.
3. Visible reproduced-state controls and reset button: complete.
4. Structured fork spawn log: complete.
5. Multi-level fork tree: complete.
6. Checking agent: complete.
7. Memory-state probe: represented conservatively; live proof requires fork/pause-capable Daytona support.
8. Live Daytona capability probe: complete for the provided key; live fork path is blocked by current account/runner capabilities.
9. Screenshots and sandbox cleanup decision: pending updated screenshot capture, either of blocked-capability UI or rerun with fork-capable Daytona support.

## Milestone 13: Live Daytona capability probe and revised remaining plan

- What changed: used the provided Daytona API key through stdin/env only to explore real account capabilities, added latency/capability findings, made the live runner record a clear blocked fork-capability state instead of crashing, and revised docs/checklists so the remaining plan distinguishes what this key can prove from what requires a fork-capable Daytona runner.
- Files created/modified: `logs/daytona-capability-probe.json`, `logs/daytona-live-latency.json`, `logs/daytona-live-run.txt`, `data/forking-console-state.json`, `scripts/daytona-live-demo.js`, `README.md`, `DEMO.md`, `docs/forking-semantics.md`, `docs/screenshot-checklist.md`, `../runs/coupon-demo-20260626-114414.md`, `SUMMARY.md`.
- Commands run: live Daytona read-only config/snapshot/sandbox probes; live default container create/prepare/fork attempt with cleanup; `daytona-vm-small` create attempts in `us` and `eu`; temporary `linux-vm` snapshot probes in `us` and `eu`; default container stop/start/snapshot probe; cleanup polling for a snapshotting sandbox; `node --check scripts/daytona-live-demo.js`; JSON parse check for `logs/daytona-capability-probe.json`; `npm run daytona:plan`; `npm run daytona:live` with `DAYTONA_CLEANUP=1`; `rg` secret/path scan for the raw key.
- Test results: `node --check scripts/daytona-live-demo.js` passed; `logs/daytona-capability-probe.json` parses; `npm run daytona:plan` passed; patched `npm run daytona:live` now exits cleanly with `blocked` status after parent preparation when fork is unsupported and deletes the parent sandbox. The raw API key was not found in repo files by `rg`.
- Screenshots captured: none in this milestone.
- Remaining TODOs: capture an updated screenshot of the root console showing the blocked live fork-capability state, or rerun the demo with a fork-capable Daytona target/snapshot and capture real sandbox IDs for the parent, hypothesis forks, fork-a-fork refinements, validators, checking agent, and memory probe.
- Assumptions/blockers: with the provided key on 2026-07-02, default container sandboxes in `us` support create/prepare/stop/start but not fork or pause; prebuilt `daytona-vm-small` is listed but unavailable in `us`/`eu`; creating a temporary `linux-vm` snapshot is blocked because `us` has no `linux-vm` runner and `eu` is unavailable to the organization; container filesystem snapshot eventually completed but exceeded a 180-second client timeout. A final post-run read-only verification command was interrupted before the key was entered, but the patched live runner reported successful deletion of the created parent sandbox.

## Milestone 14: Root npm wrapper for running the coupon demo

- What changed: added a root-level `package.json` in `daytona-forking-exploration` so `npm run dev` and common demo scripts work from the workspace root by forwarding to `daytona-forking-coupon-demo`.
- Files created/modified: `../package.json`, `SUMMARY.md`.
- Commands run: `ls`, root `package.json` check, `sed -n ... daytona-forking-coupon-demo/package.json`, `npm run`, JSON parse check for root `package.json`, root `npm run build`, `curl http://localhost:4173/`, inspected `src/server/index.js`.
- Test results: root `npm run` now lists `dev`; root `npm run build` passed and rebuilt coupon demo artifacts; `curl http://localhost:4173/` returned HTTP 200.
- Screenshots captured: none.
- Remaining TODOs: none for the root wrapper.
- Assumptions/blockers: the demo server was already running on port 4173, so no duplicate server was started.

## Milestone 15: Dev server port conflict handling

- What changed: updated the coupon demo dev server to bind to `127.0.0.1` and retry the next port when the default port is already in use, so a second `npm run dev` from the root no longer crashes with an unhandled `EADDRINUSE`.
- Files created/modified: `src/server/index.js`, `SUMMARY.md`.
- Commands run: `rg` for port/server references, inspected `src/server/index.js` and `src/server/app.js`, `node --check daytona-forking-coupon-demo/src/server/index.js`, root `npm run build`, `curl http://localhost:4173/`, root `npm run dev` in the managed sandbox, root `npm run dev` with local-server approval, stopped the verification server with Ctrl-C.
- Test results: syntax check passed; root `npm run build` passed; approved root `npm run dev` started successfully and printed `Coupon checkout demo running at http://localhost:4173`; the verification server was stopped cleanly.
- Screenshots captured: none.
- Remaining TODOs: if the user's existing terminal still owns port 4173, rerunning `npm run dev` should now print a fallback URL such as `http://localhost:4174`; use that URL or stop the earlier server.
- Assumptions/blockers: the managed tool sandbox reports `EPERM` for local port binding without approval, so the actual server-start validation used an approved local-server command.

## Milestone 16: Browser-started blocked run cleanup

- What changed: changed browser-started Daytona runs so they pass `DAYTONA_CLEANUP=1` by default unless the local server is explicitly started with a different cleanup setting. Updated docs and visible console state so blocked fork runs do not imply the parent sandbox is still kept by default.
- Files created/modified: `src/server/app.js`, `data/forking-console-state.json`, `README.md`, `DEMO.md`, `SUMMARY.md`.
- Commands run: inspected `src/server/app.js`, `src/client/forking-demo.js`, `logs/daytona-live-run.txt`, and current `data/forking-console-state.json`; used the Daytona API key through stdin to delete sandbox `bce2b368-0c0c-45dd-b9c3-eb2b16159c4f`; polled Daytona until the sandbox was no longer returned; `node --check src/server/app.js`; JSON parse check for `data/forking-console-state.json`; root `npm run build`; `rg` scan for raw key and stale cleanup wording.
- Test results: Daytona cleanup succeeded; final poll returned no matching sandbox; `src/server/app.js` syntax check passed; `data/forking-console-state.json` parses; root `npm run build` passed. The raw API key was not found in repo files by the scan.
- Screenshots captured: none.
- Remaining TODOs: restart the local dev server so the running browser path picks up the new cleanup default.
- Assumptions/blockers: if an older server process is still running on port 4173, it still has the old environment/code in memory; stop it or use the new fallback-port server URL after restart.

## Milestone 17: Daytona API key preflight and input cleanup

- What changed: added a server-side Daytona credential preflight before spawning the live runner, so invalid keys fail immediately without creating a sandbox. Updated the browser flow to refresh the Live Daytona Run Log after preflight errors. Fixed the API key input placeholder from `daytona_...` to the observed `dtn_...` format and disabled browser autocorrect/capitalization on the key field.
- Files created/modified: `src/server/app.js`, `src/client/forking-demo.js`, `src/client/index.html`, `src/client/forking-demo.html`, `README.md`, `DEMO.md`, `SUMMARY.md`.
- Commands run: `rg` for key/live-run paths, inspected `src/server/app.js`, `src/client/forking-demo.js`, `src/client/index.html`, and `src/client/forking-demo.html`; `node --check src/server/app.js`; `node --check src/client/forking-demo.js`; root `npm run build`; `rg` verification for `dtn_...`, `verifyDaytonaCredentials`, and preflight docs.
- Test results: server syntax check passed; client syntax check passed; root `npm run build` passed. The old `daytona_...` placeholder is no longer present in the checked source paths.
- Screenshots captured: none.
- Remaining TODOs: restart the local dev server before retrying the browser flow, then paste the full valid `dtn_...` key again. If the preflight still says invalid credentials, create or use a fresh Daytona key.
- Assumptions/blockers: the latest browser log showed Daytona rejecting the key before sandbox creation. This is now surfaced as a preflight credential error instead of a spawned live-run failure.

## Milestone 18: Terminal-first root demo

- What changed: changed the root page so only the coupon bug reproduction stays as browser UI; the forking demo now runs through an operator-console presentation. Added a parent terminal, a redacted live stream terminal, and a four-pane split for `frontend-hypothesis`, `backend-hypothesis`, `contract-hypothesis`, and `checking-agent`. Hid the older visual tree/matrix/card panels on the root page while keeping `/forking-demo` as the visual reference view.
- Files created/modified: `src/client/index.html`, `src/client/forking-demo.js`, `src/client/forking-demo.css`, `README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `../runs/coupon-demo-20260626-114414.md`, `SUMMARY.md`.
- Commands run: inspected root HTML, CSS, JS, visual reference page, and state JSON; `node --check src/client/forking-demo.js`; JSON parse check for `data/forking-console-state.json`; root `npm run build`; `rg` verification for operator-console selectors and docs.
- Test results: client syntax check passed; state JSON parses; root `npm run build` passed.
- Screenshots captured: none.
- Remaining TODOs: restart the dev server to view the new root console; capture a new root operator-console screenshot if this becomes the post’s primary visual.
- Assumptions/blockers: `/forking-demo` intentionally remains the visual/reference version, while `/` is now the terminal-first demo surface.

## Milestone 19: Video recording terminal flow with latency metrics

- What changed: added a recording-oriented terminal flow. The browser now cues the handoff after coupon reproduction, while `npm run video:terminal` waits for `data/demo-session-state.json`, then animates parent prep, stop/start proof, snapshot baseline, three hypothesis forks, contract candidate-fix, fork-a-fork refinements, validator fan-out, checking-agent decision, and a conservative runtime-state observation. The terminal shows per-step latency and writes `logs/video-terminal-metrics.json`.
- Files created/modified: `scripts/video-reset.js`, `scripts/video-terminal-demo.js`, `package.json`, `../package.json`, `src/client/index.html`, `src/client/forking-demo.js`, `src/client/forking-demo.css`, `dist/client/index.html`, `dist/client/forking-demo.js`, `dist/client/forking-demo.css`, `dist/build-info.json`, `README.md`, `../README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `../runs/coupon-demo-20260626-114414.md`, `logs/video-terminal-metrics.json`, `SUMMARY.md`.
- Commands run: `node --check scripts/video-reset.js`, `node --check scripts/video-terminal-demo.js`, `node --check src/client/forking-demo.js`, `node --check src/server/app.js`, root `npm run build`, root `npm run video:reset`, `VIDEO_TERMINAL_FAST=1 VIDEO_TERMINAL_SKIP_WAIT=1 VIDEO_TERMINAL_NO_CLEAR=1 npm run video:terminal`, JSON parse/summary check for `logs/video-terminal-metrics.json`, attempted `git status --short`.
- Test results: all syntax checks passed; root build passed; `npm run video:reset` reset the reproduction gate; fast-mode terminal validation completed 17 operations and wrote a parseable metrics file. `git status --short` could not run because the current workspace root is not a Git repository.
- Screenshots captured: none.
- Remaining TODOs: for the actual recording, run `npm run video:reset`, start `DAYTONA_API_KEY=... npm run video:terminal` without the skip-wait flag, reproduce the bug in the browser, and capture the terminal segment. Capture fresh screenshots afterward if these visuals are used in the written guide.
- Assumptions/blockers: the new terminal flow is deterministic recording mode and does not create live Daytona forks. It displays only whether the API key is configured, not the key itself. The optional browser-started Daytona run remains available for capability checks, but live fork IDs still require a fork-capable Daytona target.

## Milestone 20: Full post-reproduction video flow test and latency screenshots

- What changed: ran the full recording flow with `DAYTONA_API_KEY` configured and redacted, reproduced the coupon bug through the local server, captured full-speed post-reproduction terminal latency, generated a markdown latency report, and added three new post-ready screenshot artifacts for the video terminal and latency report. The metrics JSON now records both total terminal wall time and wall time after the coupon bug is detected.
- Files created/modified: `scripts/video-terminal-demo.js`, `scripts/video-latency-report.js`, `scripts/capture-screenshots.js`, `package.json`, `../package.json`, `README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `docs/screenshots/manifest.md`, `docs/screenshots/pages/10-video-terminal-waiting.html`, `docs/screenshots/pages/11-video-terminal-complete.html`, `docs/screenshots/pages/12-video-latency-report.html`, `docs/screenshots/10-video-terminal-waiting.png`, `docs/screenshots/11-video-terminal-complete.png`, `docs/screenshots/12-video-latency-report.png`, `logs/video-terminal-metrics.json`, `logs/video-terminal-latency-report.md`, `logs/video-terminal-full-run.log`, `data/demo-session-state.json`, `dist/client/*`, `../runs/coupon-demo-20260626-114414.md`, `SUMMARY.md`.
- Commands run: attempted in-app browser connection; `curl http://127.0.0.1:4173/api/health`; `node --check scripts/video-terminal-demo.js`; `node --check scripts/video-latency-report.js`; `node --check scripts/capture-screenshots.js`; `node --check src/client/forking-demo.js`; `npm run video:reset`; started `npm run video:terminal` with `DAYTONA_API_KEY` supplied via terminal input and `VIDEO_TERMINAL_NO_CLEAR=1`; attempted a Node `fetch` reproduction check; `curl` validate-coupon, checkout, and reproduction-event calls for `SUMMER-2026`; `npm run video:latency-report`; parsed `logs/video-terminal-metrics.json`; inspected `logs/video-terminal-latency-report.md` and `logs/video-terminal-full-run.log`; `rg -n "dtn_[A-Za-z0-9]{40,}" ...`; `npm run capture-screenshots`; `qlmanage -t -s 1280 -o /private/tmp/daytona-video-screens ...`; copied rendered PNGs into `docs/screenshots`; `file` and `ls -lh` checks for new PNGs; visual inspection of screenshots; root `npm run build`; final JSON parse checks.
- Test results: full-speed terminal run completed successfully after the server-side reproduction marker. Metrics: `apiKeyConfigured: true`, `fastMode: false`, `operationCount: 17`, post-reproduction wall-clock time `6.56s`, terminal-start wall time `38.71s` including waiting for reproduction, sum of observed operation latencies `9.87s`, slowest single operation `868ms` for spawning `checking-agent`. `npm run video:latency-report`, `npm run capture-screenshots`, and root `npm run build` passed. Final secret-shaped scan found no Daytona API key in repo files.
- Screenshots captured: `docs/screenshots/10-video-terminal-waiting.png`, `docs/screenshots/11-video-terminal-complete.png`, `docs/screenshots/12-video-latency-report.png`; all are valid 1280x1280 PNGs and were visually checked.
- Remaining TODOs: run `npm run video:reset` before a fresh recorded take if you want the terminal to wait for browser reproduction again. Live Daytona fork IDs still require a fork-capable Daytona runner/snapshot; this recording flow remains deterministic and does not create live fork sandboxes.
- Assumptions/blockers: the in-app browser target was unavailable in this session and Playwright is not installed, so screenshots were generated from deterministic HTML pages and rendered with Quick Look. A Node `fetch` local reproduction check was blocked by sandbox networking (`EPERM`), so the same local server flow was verified with approved `curl` calls.

## Milestone 21: Simplified command-driven recording flow

- What changed: replaced the moving terminal animation as the recommended recording path with static `demo:*` commands that print once and exit. The browser page now explicitly marks where the bug appears after `Apply` and after `Checkout`. Added a command-driven script that gates on the reproduced browser marker, prints the prepared parent state, prints the first fork point, writes `fix-candidate.md`, prints the fork-a-fork contract refinements, prints validator/checking-agent fan-out, and writes a simple latency report. Updated docs, screenshot checklist, generated screenshot pages, and PNG screenshots to reflect the simpler flow.
- Files created/modified: `scripts/simple-demo.js`, `scripts/video-reset.js`, `scripts/capture-screenshots.js`, `package.json`, `../package.json`, `src/client/index.html`, `src/client/forking-demo.js`, `src/client/styles.css`, `README.md`, `../README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `docs/screenshots/manifest.md`, `docs/screenshots/pages/10-video-terminal-waiting.html`, `docs/screenshots/pages/11-video-terminal-complete.html`, `docs/screenshots/pages/12-video-latency-report.html`, `docs/screenshots/10-video-terminal-waiting.png`, `docs/screenshots/11-video-terminal-complete.png`, `docs/screenshots/12-video-latency-report.png`, `logs/simple-demo-run.json`, `logs/simple-demo-latency-report.md`, `fix-candidate.md`, `dist/client/*`, `../runs/coupon-demo-20260626-114414.md`, `SUMMARY.md`.
- Commands run: `node --check scripts/simple-demo.js`, `node --check scripts/video-reset.js`, `node --check src/client/forking-demo.js`, `npm run demo:reset`, `npm run demo:state` before reproduction to verify the guard, `curl` validate-coupon, checkout, and reproduction-event calls for `SUMMER-2026`, `npm run demo:state`, `npm run demo:forks`, `npm run demo:contract`, `npm run demo:validators`, `npm run demo:report`, JSON parse checks for package/session/simple-demo files, `node --check scripts/capture-screenshots.js`, `npm run capture-screenshots`, `qlmanage -t -s 1280 ...` for the three static screenshots, `file` checks for PNGs, visual screenshot checks, stale-flow/secret-shaped `rg` scan, and root `npm run build`.
- Test results: the pre-reproduction `demo:state` command correctly exits with instructions instead of showing forks. After the reproduced marker is written, all static commands complete and print stable output. `demo:report` records 12 operations, displayed latency sum `4.95s`, and slowest displayed operation `620ms` for writing the contract candidate fix. Screenshot generation passed with the existing non-Playwright fallback, and root build passed.
- Screenshots captured: refreshed `docs/screenshots/10-video-terminal-waiting.png`, `docs/screenshots/11-video-terminal-complete.png`, and `docs/screenshots/12-video-latency-report.png` to show the static command-driven flow; all are valid 1280x1280 PNGs and were visually checked.
- Remaining TODOs: before a fresh recorded take, run `npm run demo:reset`, reproduce the browser bug, then run `npm run demo:state`, `npm run demo:forks`, `npm run demo:contract`, `npm run demo:validators`, and `npm run demo:report`. The older `video:terminal` animation still exists as an optional artifact but is no longer the recommended demo path.
- Assumptions/blockers: the command-driven flow is local/simulated for recording clarity and does not create live Daytona forks. Live fork IDs still require a fork-capable Daytona target.

## Milestone 22: Added visible Daytona SDK command shapes

- What changed: updated the static `demo:*` outputs to show concrete Daytona SDK-shaped operations alongside the fork explanation. `demo:state` now shows parent inspection commands using `parent.process.executeCommand(...)`; `demo:forks` shows three `_experimental_fork(...)` calls from the prepared parent; `demo:contract` shows inspecting `fix-candidate.md` before forking `contract-minimal` and `contract-with-regression-test`; `demo:validators` shows validator/checking-agent fork calls from the changed candidate state. Also documented the recommended recording split: terminal/code is the primary explanation surface, Daytona console is secondary proof for sandbox IDs and fork names.
- Files created/modified: `scripts/simple-demo.js`, `README.md`, `../README.md`, `DEMO.md`, `dist/client/*`, `docs/screenshots/manifest.md`, `docs/screenshots/pages/*.html`, `logs/simple-demo-latency-report.md`, `logs/simple-demo-run.json`, `SUMMARY.md`.
- Commands run: `node --check scripts/simple-demo.js`, `npm run demo:state`, `npm run demo:forks`, `npm run demo:contract`, `npm run demo:validators`, `npm run demo:report`, root `npm run build`, root `npm run capture-screenshots`.
- Test results: static command output now includes SDK-shaped inspect/fork calls. The simple report dedupes rerun metrics and returns to 12 stable operations with displayed latency sum `4.95s`; root build passed; screenshot-page generation passed with the existing no-Playwright fallback.
- Screenshots captured: no new PNGs in this milestone; screenshot HTML pages and manifest were regenerated.
- Remaining TODOs: during recording, keep Daytona console open only if it adds visual proof. Use terminal commands to explain the actual fork operation and parent contents.
- Assumptions/blockers: `_experimental_fork(...)` is shown as the SDK shape already used in the repo examples; live execution still depends on a fork-capable Daytona runner.

## Milestone 23: Hypothesis fork outcome visualization

- What changed: added `npm run demo:hypotheses` between `demo:forks` and `demo:contract`. This command prints a static outcome matrix for `frontend-hypothesis`, `backend-hypothesis`, and `contract-hypothesis`, including changed files, UI behavior, API behavior, verdict, reasoning, and SDK-shaped branch comparison test commands. Updated the browser command list, root/demo package scripts, docs, screenshot checklist, run record, simple report, and generated screenshot pages to include the new intermediate step.
- Files created/modified: `scripts/simple-demo.js`, `package.json`, `../package.json`, `src/client/index.html`, `src/client/forking-demo.js`, `README.md`, `../README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `docs/screenshots/manifest.md`, `docs/screenshots/pages/*.html`, `docs/screenshots/11-video-terminal-complete.png`, `docs/screenshots/12-video-latency-report.png`, `logs/simple-demo-run.json`, `logs/simple-demo-latency-report.md`, `dist/client/*`, `../runs/coupon-demo-20260626-114414.md`, `SUMMARY.md`.
- Commands run: `node --check scripts/simple-demo.js`, `npm run demo:hypotheses`, `npm run demo:report`, root `npm run build`, root `npm run capture-screenshots`, `qlmanage -t -s 1280 ...` for the updated static summary/latency screenshots, copy refreshed PNGs, JSON parse checks, `rg` checks for `demo:hypotheses`, and `file` checks for updated PNGs.
- Test results: `demo:hypotheses` prints the fork outcome matrix and reasoning. `demo:report` now records 15 stable metrics, adding `evaluate hypothesis` rows for frontend/backend/contract; displayed latency sum is `5.76s`. Build passed and screenshot-page generation passed.
- Screenshots captured: refreshed `docs/screenshots/11-video-terminal-complete.png` and `docs/screenshots/12-video-latency-report.png`; both are valid 1280x1280 PNGs.
- Remaining TODOs: during recording, run `npm run demo:hypotheses` right after `npm run demo:forks` and before `npm run demo:contract` to explain why the contract fork wins.
- Assumptions/blockers: the fork outcome matrix is still a deterministic local demo artifact, not a live Daytona execution result.

## Milestone 24: Simplified hypothesis outcomes

- What changed: simplified `npm run demo:hypotheses` from a dense outcome matrix into three short decision blocks. Each fork now shows `Tried`, `Result`, and `Verdict`, followed by a simple explanation of why the contract branch wins and optional test commands to prove each branch.
- Files created/modified: `scripts/simple-demo.js`, `logs/simple-demo-run.json`, `logs/simple-demo-latency-report.md`, `dist/client/*`, `SUMMARY.md`.
- Commands run: `node --check scripts/simple-demo.js`, `npm run demo:hypotheses`, `npm run demo:report`, root `npm run build`.
- Test results: `demo:hypotheses` now prints the cleaner decision-slide-style output; report still records 15 operations and displayed latency sum `5.76s`; root build passed.
- Screenshots captured: none.
- Remaining TODOs: if screenshot artifacts for `demo:hypotheses` are needed, add a dedicated screenshot page for this command output.
- Assumptions/blockers: no live Daytona execution was changed; this remains a deterministic demo explanation.

## Milestone 25: Agent-style hypothesis reasoning

- What changed: expanded `npm run demo:hypotheses` so each hypothesis fork now includes agent-style reasoning: why the fork was plausible, what experiment it ran, what it observed, and why the result is partial or winning. The winning-contract explanation now emphasizes the state boundary where UI and API validation diverge.
- Files created/modified: `scripts/simple-demo.js`, `logs/simple-demo-run.json`, `logs/simple-demo-latency-report.md`, `dist/client/*`, `SUMMARY.md`.
- Commands run: `node --check scripts/simple-demo.js`, `npm run demo:hypotheses`, `npm run demo:report`, root `npm run build`.
- Test results: `demo:hypotheses` prints the richer agent reasoning without needing any external API key; report still records 15 operations and displayed latency sum `5.76s`; root build passed.
- Screenshots captured: none.
- Remaining TODOs: none for the reasoning text.
- Assumptions/blockers: no additional API keys are needed unless the demo later switches to live LLM-generated reasoning, which would make the recording less deterministic.

## Milestone 26: OpenAI Platform MCP and local key setup

- What changed: initialized/discovered the `codex_apps` OpenAI Platform MCP tools, verified that Platform targets can be listed, created a new OpenAI API key through the encrypted connector flow, and wrote it to the confirmed local env destination without printing the plaintext value. Added `.env` to the root ignore file.
- Files created/modified: `../.env`, `../.gitignore`, `SUMMARY.md`.
- Commands run: `tool_search` for `codex_apps`/OpenAI Platform tools; OpenAI Platform target listing; local destination confirmation; encrypted key prepare/decrypt helper; safe file existence checks; root `.gitignore` inspection; no-output `OPENAI_API_KEY` presence check.
- Test results: OpenAI Platform target listing succeeded; key creation succeeded for the Daytona organization default project; decrypt helper reported `OPENAI_API_KEY` written to `/Users/hana/Documents/Code/daytona-forking-exploration/.env` and `wrote_plaintext_to_stdout: false`; `.env` exists, is non-empty, contains an `OPENAI_API_KEY` entry, and is listed in the root `.gitignore`.
- Screenshots captured: none.
- Remaining TODOs: wire the coupon demo to use this local `OPENAI_API_KEY` for live agent-generated reasoning if we still want LLM-authored reasoning in the recording.
- Assumptions/blockers: the workspace root did not behave as a Git repository when checked with `git`, so tracked-file verification could not run. `.env` is now ignored by the root `.gitignore` for safety.

## Milestone 27: Live OpenAI agent reasoning command

- What changed: added `npm run demo:agent`, which reads `OPENAI_API_KEY` from the local environment or root `.env`, calls the OpenAI Responses API, writes live externalized debugging rationale for the three hypothesis forks, records the request latency into the simple demo run, and saves both markdown and structured JSON reports. Updated the browser command list, visual-reference copy, README/DEMO docs, screenshot checklist, screenshot generator, and manifest. Added a post-ready visual summary of the live agent reasoning.
- Files created/modified: `scripts/agent-reasoning.js`, `package.json`, `../package.json`, `scripts/simple-demo.js`, `src/client/index.html`, `src/client/forking-demo.js`, `README.md`, `../README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `scripts/capture-screenshots.js`, `docs/screenshots/manifest.md`, `docs/screenshots/pages/*.html`, `docs/screenshots/11-video-terminal-complete.png`, `docs/screenshots/12-video-latency-report.png`, `docs/screenshots/13-agent-reasoning.png`, `logs/agent-reasoning.md`, `logs/agent-reasoning.json`, `logs/simple-demo-run.json`, `logs/simple-demo-latency-report.md`, `dist/client/*`, `SUMMARY.md`.
- Commands run: read OpenAI API key and docs skills; consulted official OpenAI Responses/structured-output docs; `node --check scripts/agent-reasoning.js`; `node --check scripts/simple-demo.js`; `node --check scripts/capture-screenshots.js`; `node --check src/client/forking-demo.js`; package JSON parse checks; `npm run demo:help`; `npm run demo:agent` once in sandbox, then with network approval; two follow-up `npm run demo:agent` runs after prompt corrections; `npm run demo:report`; JSON parse checks for `logs/agent-reasoning.json` and `logs/simple-demo-run.json`; no-output secret-shaped scans; `npm run capture-screenshots`; `qlmanage -t -s 1280 ...` for screenshot rendering; `file` checks for new PNGs; visual inspection of `docs/screenshots/13-agent-reasoning.png`; root `npm run build`.
- Test results: final live `demo:agent` run succeeded with model `gpt-4.1-mini`, latency `14120ms`, and saved accurate reasoning that identifies `contract-hypothesis` as the winner. `demo:report` now records 16 operations with displayed latency sum `19880ms`; the OpenAI reasoning call is the slowest operation. Syntax checks, JSON checks, screenshot-page generation, and root build passed. Secret-shaped scan returned no matches in generated reasoning/log/screenshot-page artifacts.
- Screenshots captured: refreshed `docs/screenshots/11-video-terminal-complete.png` and `docs/screenshots/12-video-latency-report.png`; added and visually checked `docs/screenshots/13-agent-reasoning.png` as a compact model/latency/fork-reasoning visual.
- Remaining TODOs: for a recorded take, run `npm run demo:agent` after `npm run demo:hypotheses` only if live model-authored reasoning is desired; otherwise skip it and keep the deterministic static flow.
- Assumptions/blockers: outbound OpenAI API access required escalation in this Codex sandbox. The script records externalized debugging rationale, not private chain-of-thought, and avoids claiming runtime/process memory is preserved unless a Daytona runner proves it.

## Milestone 28: Simplified branch command and explicit timing source

- What changed: added `npm run demo:branch` as the recommended shorter recording command. It combines prepared parent inspection, first hypothesis forks, what happens inside each fork, why the contract branch wins, and the fork-a-fork setup into one stable terminal output. Updated `demo:forks`, `demo:contract`, `demo:validators`, `demo:report`, and screenshot captions to explicitly state that non-live millisecond values are deterministic local demo timings from `scripts/simple-demo.js`, not measured Daytona API latency. Expanded `demo:contract` with clearer explanations of what happens inside `contract-hypothesis`, why the changed contract branch becomes the next parent, and what `contract-minimal` versus `contract-with-regression-test` are meant to prove.
- Files created/modified: `scripts/simple-demo.js`, `package.json`, `../package.json`, `src/client/index.html`, `src/client/forking-demo.js`, `README.md`, `../README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `scripts/capture-screenshots.js`, `docs/screenshots/manifest.md`, `docs/screenshots/pages/*.html`, `docs/screenshots/10-video-terminal-waiting.png`, `docs/screenshots/11-video-terminal-complete.png`, `docs/screenshots/12-video-latency-report.png`, `logs/simple-demo-run.json`, `logs/simple-demo-latency-report.md`, `fix-candidate.md`, `dist/client/*`, `SUMMARY.md`.
- Commands run: `node --check scripts/simple-demo.js`; `node --check scripts/capture-screenshots.js`; `node --check src/client/forking-demo.js`; package JSON parse checks; `npm run demo:help`; `npm run demo:branch`; `npm run demo:validators`; `npm run demo:report`; `npm run demo:forks`; `npm run capture-screenshots`; `qlmanage -t -s 1280 ...` for updated screenshots; copied rendered PNGs into `docs/screenshots`; `file` checks for PNG dimensions; visual inspection of `docs/screenshots/11-video-terminal-complete.png`; no-output secret-shaped scans; root `npm run build`.
- Test results: `demo:branch` prints the shorter parent-to-contract story and writes `fix-candidate.md`; `demo:validators` works after `demo:branch`; `demo:forks` now prints the timing-source note before the fork tree; `demo:report` records 16 operations and writes the timing note into `logs/simple-demo-latency-report.md`; syntax checks, JSON checks, screenshot generation, and root build passed. Secret-shaped scan returned no matches in the generated demo/report/screenshot-page artifacts checked.
- Screenshots captured: refreshed `docs/screenshots/10-video-terminal-waiting.png`, `docs/screenshots/11-video-terminal-complete.png`, and `docs/screenshots/12-video-latency-report.png`; `11-video-terminal-complete.png` was visually checked after the caption fix.
- Remaining TODOs: if the video should avoid all simulated timing language, omit `demo:report` and narrate only the measured `demo:agent` latency plus any Daytona live probe timings.
- Assumptions/blockers: the branch/fork timings shown by `demo:branch`, `demo:forks`, `demo:contract`, and `demo:validators` are intentionally deterministic presentation values. They should not be described as real Daytona fork latency.

## Milestone 29: Separate localhost recording flow from advanced Daytona probe

- What changed: clarified the credential split that caused localhost confusion. The browser page now treats the Daytona key form as an advanced Daytona probe hidden behind a disclosure control, while the recommended recording path remains `npm run demo:branch`, optional `npm run demo:agent`, `npm run demo:validators`, and `npm run demo:report`. Reset the default visual state from a stale blocked live Daytona run to `local static`, `not required for recording`, and `prepared local`. Updated browser labels from "live run" to "Daytona probe" and changed stale `demo:state` guidance to `demo:branch`. Documented that `npm run demo:agent` uses the OpenAI key from `.env`, while the advanced localhost probe expects a Daytona `dtn_...` key.
- Files created/modified: `src/client/index.html`, `src/client/forking-demo.html`, `src/client/forking-demo.css`, `src/client/forking-demo.js`, `data/forking-console-state.json`, `README.md`, `../README.md`, `DEMO.md`, `dist/client/*`, `SUMMARY.md`.
- Commands run: searched browser/server code for Daytona form and live-run references; inspected `data/forking-console-state.json`; patched default state and UI copy; `node --check src/client/forking-demo.js`; JSON parse checks for `data/forking-console-state.json`; root `npm run build`; `curl` checks for `/api/health`, `/api/forking-state`, and `/`; `lsof -nP -iTCP:4173 -sTCP:LISTEN`; attempted Playwright screenshot capture through bundled Chromium and system Chrome; stale-wording `rg` checks.
- Test results: build passed; localhost `/api/health` returned ok; localhost `/api/forking-state` now reports `mode: local static`, `liveStatus: not required for recording`, `parentStatus: prepared local`, `level0Status: prepared local`, and `terminalTitle: advanced Daytona probe`; root HTML now contains `Advanced Daytona live capability check`, `npm run demo:branch`, and `OpenAI in .env; Daytona optional`, with no stale `Start optional live run` or main-flow `demo:state` guidance.
- Screenshots captured: none. Attempted Playwright capture of the updated localhost page, but the bundled Playwright browser was not installed and system Chrome failed to launch under the sandbox with an EPERM-related close/kill error.
- Remaining TODOs: hard refresh the existing browser tab at `http://localhost:4173/` so cached JS/HTML is replaced. If you need a new screenshot of the main localhost page, run it from a normal browser or install Playwright browsers outside this sandbox.
- Assumptions/blockers: the advanced Daytona probe can still show `Invalid credentials` if a bad or revoked Daytona `dtn_...` key is submitted. That is separate from the local demo and from `npm run demo:agent`, which uses the OpenAI API key.

## Milestone 30: Cleared stuck localhost listener

- What changed: stopped the lingering Node listener on port `4173` so the local demo can be restarted from a normal terminal.
- Files created/modified: `SUMMARY.md`.
- Commands run: `lsof -nP -iTCP:4173 -sTCP:LISTEN`; `kill 96458`; re-ran `lsof -nP -iTCP:4173 -sTCP:LISTEN`; attempted `npm run dev` from the Codex sandbox.
- Test results: after `kill 96458`, `lsof` returned no listener on `4173`, so the port is free. Starting `npm run dev` from this sandbox failed with `EPERM` while binding `127.0.0.1:4173`, which is a sandbox bind restriction rather than a remaining local listener.
- Screenshots captured: none.
- Remaining TODOs: start the server from the user terminal with `npm run dev`, then hard refresh `http://localhost:4173/`.
- Assumptions/blockers: Codex cannot reliably bind the dev server in this managed sandbox, but the user terminal should be able to now that the stale listener is gone.

## Milestone 31: Cleaned recording-first browser flow

- What changed: simplified the main localhost page for recording. The browser now focuses on reproducing the coupon bug and handing off to stable terminal commands. Hidden from the main page: mode/probe/credential status cards, the advanced Daytona probe form, internal server-state details, and the synthetic live-stream fallback that showed `[documented] coupon-parent-prepared <- prepared local state`. The command block now labels the exact recording order: keep `npm run dev` running, reset before the take, reproduce the bug, run `demo:branch`, optionally run `demo:agent`, then finish with `demo:validators` and `demo:report`.
- Files created/modified: `src/client/index.html`, `src/client/forking-demo.js`, `src/client/forking-demo.css`, `dist/client/*`, `dist/build-info.json`, `logs/simple-demo-run.json`, `logs/simple-demo-latency-report.md`, `logs/agent-reasoning.md`, `logs/agent-reasoning.json`, `fix-candidate.md`, `state-manifest.json`, `data/demo-session-state.json`, `SUMMARY.md`.
- Commands run: `node --check src/client/forking-demo.js`; root `npm run build`; root `npm run demo:branch`; root `npm run demo:validators`; root `npm run demo:report`; root `npm run test`; `rg` checks for the cleaned command labels and hidden root-page selectors; attempted `git diff -- ...` which failed because this workspace root is not a Git repository; `find daytona-forking-coupon-demo -type f -mmin -15`.
- Test results: JavaScript syntax check passed. Build passed and wrote `dist/client`. `demo:branch`, `demo:validators`, and `demo:report` all completed and printed the simplified fork story. `npm run test` failed with the expected baseline coupon bug assertions: unknown coupon validation still returns accepted state, e2e invalid coupon rejection fails, and UI inline rejection fails. These red tests preserve the bug for the demo.
- Screenshots captured: none.
- Remaining TODOs: hard refresh `http://localhost:4173/` after restarting or while the server is running so the hidden status/probe blocks disappear from the visible page. Use `/forking-demo` only when the full reference/operator view is useful.
- Assumptions/blockers: the primary video path is now local and command-driven. The advanced Daytona probe remains available on the full visual reference page, but it is no longer part of the main browser recording UI.

## Milestone 32: Codex-style hypothesis forking command

- What changed: added `npm run demo:codex` as the primary post-browser recording flow. The command accepts a bug/hypothesis prompt, verifies the browser reproduction marker, shows prepared parent state, prints Daytona SDK-shaped fork calls, fans out `frontend-hypothesis`, `backend-hypothesis`, and `contract-hypothesis`, tests each fork with clear pass/fail reasoning, selects the shared contract/schema hypothesis, writes `fix-candidate.md`, then forks the changed contract state into refinements, validators, and a checking agent. Updated the main browser command handoff, `/forking-demo` copy, built-in help, README/DEMO docs, package scripts, and screenshot checklist to make `demo:codex` the recommended path.
- Files created/modified: `scripts/codex-fork-demo.js`, `scripts/simple-demo.js`, `package.json`, `../package.json`, `src/client/index.html`, `src/client/forking-demo.html`, `src/client/forking-demo.js`, `README.md`, `../README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `logs/codex-fork-session.md`, `logs/codex-fork-session.json`, `logs/simple-demo-run.json`, `logs/simple-demo-latency-report.md`, `fix-candidate.md`, `dist/client/*`, `dist/build-info.json`, `state-manifest.json`, `data/demo-session-state.json`, `SUMMARY.md`.
- Commands run: `node --check scripts/codex-fork-demo.js`; `node --check scripts/simple-demo.js`; `node --check src/client/forking-demo.js`; package JSON parse check; `npm run demo:help`; `npm run demo:codex -- --default`; `npm run demo:codex -- "I have a checkout coupon bug. Hypotheses: frontend validation, backend API validation, shared coupon contract. Fork the sandboxes and test which is correct."`; JSON parse checks for `logs/codex-fork-session.json` and `logs/simple-demo-run.json`; `sed` inspection of `logs/simple-demo-latency-report.md`; `rg` checks for `demo:codex` wiring; root `npm run build`; root `npm run test`; `find daytona-forking-coupon-demo -type f -mmin -20`.
- Test results: syntax checks and package JSON checks passed. `demo:help` now recommends `demo:codex`. The quoted prompt run completed and wrote `logs/codex-fork-session.md`, `logs/codex-fork-session.json`, `logs/simple-demo-latency-report.md`, and `fix-candidate.md`; displayed demo latency total is `5772ms`. Build passed and wrote `dist/client`. `npm run test` failed in the expected three baseline bug assertions: API unknown-coupon rejection, e2e early invalid-coupon rejection, and UI inline invalid-coupon rejection.
- Screenshots captured: none.
- Remaining TODOs: for the cleanest recording, run `npm run demo:codex` without `--default` and press Enter at the prompt, or pass the quoted prompt directly. The root wrapper produced an npm warning for the test-only `--default` flag, so do not use that flag in the recorded take.
- Assumptions/blockers: `demo:codex` is a deterministic Codex-style recording transcript, not a live Daytona fork execution. It labels the fork/test timings as demo timings unless replaced by real live Daytona measurements from a fork-capable runner.

## Milestone 33: Removed non-live checking agent from the demo

- What changed: removed the checking-agent/fourth-worker concept from the active demo because it was not live in the recommended recording path. The flow now has three hypothesis forks, two contract refinements, three validator branches, and a plain validator decision rule. Removed the checking-agent pane/panel from the browser pages, removed the standalone `checking-agent-demo` command and script, removed stale checker artifacts, cleaned the live Daytona runner so it no longer forks a checker sandbox, updated docs/graphs/screenshot pages, and regenerated the Codex transcript and latency report.
- Files created/modified/deleted: `scripts/codex-fork-demo.js`, `scripts/simple-demo.js`, `scripts/agent-reasoning.js`, `scripts/daytona-live-demo.js`, `scripts/video-terminal-demo.js`, `scripts/video-latency-report.js`, `scripts/capture-screenshots.js`, `scripts/render-graphs.js`, `scripts/fork-tree-demo.js`, `scripts/validator-fanout-demo.js`, deleted `scripts/checking-agent-demo.js`, `package.json`, `../package.json`, `src/client/index.html`, `src/client/forking-demo.html`, `src/client/forking-demo.js`, `src/client/forking-demo.css`, `data/forking-console-state.json`, `README.md`, `../README.md`, `DEMO.md`, `docs/forking-semantics.md`, `docs/screenshot-checklist.md`, `docs/graphs/*.svg`, `docs/screenshots/manifest.md`, `docs/screenshots/pages/*.html`, deleted `docs/screenshots/pages/09-checking-agent.html`, deleted `docs/screenshots/09-checking-agent.png`, deleted `checking-agent-report.md`, deleted `validator-summary.json`, deleted stale `logs/video-terminal-full-run.log`, `logs/video-terminal-metrics.json`, and `logs/video-terminal-latency-report.md`, updated `logs/codex-fork-session.*`, `logs/agent-reasoning.*`, `logs/simple-demo-run.json`, `logs/simple-demo-latency-report.md`, `dist/client/*`, `dist/build-info.json`, `SUMMARY.md`.
- Commands run: `rg` scans for checking-agent/checker references; syntax checks for `scripts/codex-fork-demo.js`, `scripts/simple-demo.js`, `scripts/agent-reasoning.js`, `scripts/daytona-live-demo.js`, `scripts/video-terminal-demo.js`, and `scripts/capture-screenshots.js`; JSON parse checks for package files, `data/forking-console-state.json`, `logs/codex-fork-session.json`, and `logs/simple-demo-run.json`; `npm run demo:codex -- "I have a checkout coupon bug. Hypotheses: frontend validation, backend API validation, shared coupon contract. Fork the sandboxes and test which is correct."`; attempted root `npm run render-graphs` which failed because the root wrapper has no such script; `npm --prefix daytona-forking-coupon-demo run render-graphs`; `npm run capture-screenshots`; `npm run build`; `npm run demo:validators`; `npm run demo:report`; `npm run test`; `rm` for stale checker PNG/report/log artifacts; final stale-term scans excluding `SUMMARY.md` and dependencies.
- Test results: syntax checks passed; JSON checks passed; `demo:codex` passed and now shows three validators plus a validator decision rule with displayed demo latency total `5252ms`; `demo:validators` passed and no longer prints a checking-agent fork; `demo:report` passed with 15 operations and no checker rows; graph rendering passed; screenshot HTML/manifest generation passed but did not capture new PNGs because Playwright is not installed; build passed. `npm run test` still fails in the expected three baseline coupon-bug assertions.
- Screenshots captured: none. `docs/screenshots/09-checking-agent.png` was deleted because it represented the removed non-live concept; screenshot HTML pages and manifest were regenerated.
- Remaining TODOs: if PNG screenshots are needed, regenerate them from the updated HTML pages in an environment with Playwright or via the existing manual renderer. Historical `SUMMARY.md` milestones still mention the old checker work as project history; active source/docs/logs/dist no longer do.
- Assumptions/blockers: removing the checker is a demo-clarity choice. The current narrative should not claim a fourth live worker; it should say the validator decision is a rule applied to the three validator branch results.

## Milestone 34: Simplified browser demo and generalized Codex prompt

- What changed: removed the visible Step 2 terminal-command panel from the root browser demo, including the terminal command list, reset button text, and visual-reference handoff copy. The browser now focuses on the reproduced coupon bug and captured parent state. Hid the root utility nav for a cleaner recording frame while keeping `/forking-demo` available directly. Updated the Codex-style prompt from "Describe the bug and the three hypotheses" to "The parent state with the bug has been captured. What hypotheses should I fork and test from here?" so the interaction feels reusable for other debugging scenarios.
- Files created/modified: `src/client/index.html`, `src/client/styles.css`, `scripts/codex-fork-demo.js`, `README.md`, `../README.md`, `DEMO.md`, `logs/codex-fork-session.md`, `logs/codex-fork-session.json`, `logs/simple-demo-run.json`, `logs/simple-demo-latency-report.md`, `fix-candidate.md`, `dist/client/*`, `dist/build-info.json`, `SUMMARY.md`.
- Commands run: inspected root page, styles, and Codex prompt source; `rg` checks for removed Step 2/terminal/prompt phrases in source and built files; `node --check scripts/codex-fork-demo.js`; `node --check src/client/forking-demo.js`; `npm run demo:codex -- "Hypotheses: frontend validation, backend API validation, shared coupon contract. Fork the captured parent state and test which is correct."`; root `npm run build`; root `npm run test`; `find daytona-forking-coupon-demo -type f -mmin -20`.
- Test results: stale phrase scan returned no matches for the removed Step 2 block or old prompt wording in root source/built files. Syntax checks passed. `demo:codex` passed with the more general captured-parent-state request and wrote refreshed Codex session artifacts. Build passed. `npm run test` still fails in the expected three baseline coupon-bug assertions.
- Screenshots captured: none.
- Remaining TODOs: hard refresh `http://localhost:4173/` during the next recording take so the browser no longer shows the old Step 2 panel from cache.
- Assumptions/blockers: the root page keeps hidden form elements needed by the shared browser script, but they are not visible in the recording UI and the command-copy text has been removed from the root HTML.

## Milestone 35: Live Daytona timing rows for sandbox operations

- What changed: added a terminal-first live timing path with `npm run demo:codex:live`. The Codex-style prompt can now hand off to the live Daytona runner, which prints `[live:start]`, `[live:ok]`, and `[live:failed]` rows as measured SDK operations run. The existing `demo:codex` flow remains deterministic for stable video pacing. Updated docs and built-in help to explain that `demo` millisecond values are simulated while `demo:codex:live` writes measured timings to `logs/daytona-live-latency.json`.
- Files created/modified: `scripts/daytona-live-demo.js`, `scripts/codex-fork-demo.js`, `scripts/simple-demo.js`, `package.json`, `../package.json`, `README.md`, `../README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `logs/codex-fork-session.md`, `logs/codex-fork-session.json`, `logs/simple-demo-run.json`, `logs/simple-demo-latency-report.md`, `fix-candidate.md`, `SUMMARY.md`.
- Commands run: inspected the live runner, Codex-style demo script, package scripts, README/DEMO docs, screenshot checklist, and prior summary; `node --check daytona-forking-coupon-demo/scripts/codex-fork-demo.js`; `node --check daytona-forking-coupon-demo/scripts/daytona-live-demo.js`; package JSON parse checks; `npm run demo:help`; root `npm run build`; root `npm run test`; `npm run daytona:plan`; `npm run demo:codex -- --default`; attempted `git diff` and `git status`, both blocked because the workspace root is not a Git repository.
- Test results: syntax checks passed. Package JSON files parse. `npm run demo:help` now lists `npm run demo:codex:live`. `npm run build` passed. `npm run daytona:plan` passed and now points to `npm run demo:codex:live` as the live command while reporting no Daytona key configured in this shell. `npm run demo:codex -- --default` passed with no npm argument-forwarding warning after the root wrapper fix. `npm run test` still fails in the expected three baseline coupon-bug assertions.
- Screenshots captured: none.
- Remaining TODOs: run `npm run demo:codex:live` in a terminal with a valid `DAYTONA_API_KEY` and a fork-capable Daytona runner to capture real live fork timings and update screenshots if needed.
- Assumptions/blockers: no live Daytona sandbox run was started during this milestone to avoid creating resources from the current shell. The live path will still show measured failure timing if credentials are missing/invalid or if the runner lacks fork support; a complete live fork tree still requires fork-capable Daytona support.

## Milestone 36: Clean Daytona API key setup failure for live timing path

- What changed: fixed the `npm run demo:codex:live` missing-key failure so it no longer prints a Node stack trace. The live runner now reads Daytona credentials from the shell, `daytona-forking-coupon-demo/.env.daytona.local`, `daytona-forking-coupon-demo/.env`, workspace-root `.env.daytona.local`, or workspace-root `.env`, then emits a concise setup message if `DAYTONA_API_KEY` is still missing. The blocked credential case now writes `logs/daytona-live-run.txt` and `logs/daytona-live-latency.json` with a clear failed credential operation.
- Files created/modified: `scripts/daytona-live-demo.js`, `README.md`, `../README.md`, `DEMO.md`, `logs/daytona-live-run.txt`, `logs/daytona-live-latency.json`, `dist/client/*`, `dist/build-info.json`, `SUMMARY.md`.
- Commands run: inspected the live runner credential check, env examples, README/DEMO references, and script footer; `node --check daytona-forking-coupon-demo/scripts/daytona-live-demo.js`; `DAYTONA_API_KEY= npm run demo:codex:live -- --default`; root `npm run build`; JSON parse check for `logs/daytona-live-latency.json`; root `npm run test`; inspected `logs/daytona-live-run.txt` and `logs/daytona-live-latency.json`.
- Test results: syntax check passed. The forced-empty-key live command exited with a concise setup message and no stack trace, did not create Daytona resources, wrote `[blocked] DAYTONA_API_KEY is not configured.` to the live run log, and wrote a valid latency JSON entry with operation `configure Daytona credentials` and status `failed`. Build passed. `npm run test` still fails in the expected three baseline coupon-bug assertions.
- Screenshots captured: none.
- Remaining TODOs: put the Daytona key in one supported location, preferably `daytona-forking-coupon-demo/.env.daytona.local` or workspace-root `.env.daytona.local`, then run `npm run demo:codex:live` without forcing `DAYTONA_API_KEY=` to capture live timings.
- Assumptions/blockers: the verification intentionally forced `DAYTONA_API_KEY` to an empty value so env files could not accidentally trigger a live sandbox run during this fix.

## Milestone 37: Diagnose blank Daytona env files explicitly

- What changed: improved the live Daytona credential diagnostics after copying `.env.daytona.example` to `.env.daytona.local`. Blank `DAYTONA_API_KEY=` entries are now tracked as diagnostics but are not loaded as credentials, so a copied blank demo env file cannot mask a real key in a later root env file. `npm run demo:codex:live` now says which env file contains the blank key, and `npm run daytona:check` reports `DAYTONA_API_KEY: blank in daytona-forking-coupon-demo/.env.daytona.local`.
- Files created/modified: `scripts/daytona-live-demo.js`, `logs/daytona-live-latency.json`, `dist/client/*`, `dist/build-info.json`, `SUMMARY.md`.
- Commands run: redacted local env inspection confirming `daytona-forking-coupon-demo/.env.daytona.local` exists with a present but empty `DAYTONA_API_KEY` value; inspected `scripts/daytona-live-demo.js`; `node --check daytona-forking-coupon-demo/scripts/daytona-live-demo.js`; `DAYTONA_API_KEY= npm run demo:codex:live -- --default`; `npm run daytona:check`; root `npm run build`; inspected `logs/daytona-live-latency.json`.
- Test results: syntax check passed. Forced-empty-key live run now reports the blank env file directly and still avoids creating Daytona resources. `daytona:check` reports the blank key source. Build passed. The existing blocked latency JSON remains valid.
- Screenshots captured: none.
- Remaining TODOs: edit `daytona-forking-coupon-demo/.env.daytona.local` so it contains `DAYTONA_API_KEY=dtn_...`, then run `npm run daytona:check` before `npm run demo:codex:live`.
- Assumptions/blockers: no real Daytona key was printed or inspected; the local check only reported whether the value was empty or non-empty.

## Milestone 38: Live Daytona key configured, fork capability blocked

- What changed: recorded the first successful live Daytona timing run with credentials configured. The live runner created a parent sandbox and prepared the coupon parent state, then blocked on the first fork because the current sandbox class does not support forking.
- Files created/modified: `SUMMARY.md`.
- Commands run: user ran `npm run demo:codex:live` after configuring `DAYTONA_API_KEY`.
- Test results: live measured timings from the reported run: `create parent sandbox` completed in `332ms`; `prepare parent state` completed in `453ms`; `fork frontend-hypothesis` failed in `111ms` with `Forking is not supported for this sandbox`. The live runner kept parent sandbox `72bc4209-0a81-42c1-9787-b18c0318aed7` for inspection.
- Screenshots captured: none.
- Remaining TODOs: either use a fork-capable Daytona runner/snapshot for real fork IDs, or keep `demo:codex` as the recording path for the fork tree and use this live run only as a capability/latency proof for parent creation and preparation.
- Assumptions/blockers: this is a Daytona runner capability blocker, not a local script failure. The current live run proves credential loading and parent preparation, but not live fork support.

## Milestone 39: Investigated why live fork is blocked

- What changed: investigated the live fork blocker and confirmed the failure is not caused by Daytona credentials or missing SDK support. The live run reaches `daytona.create(...)`, executes commands in the parent sandbox, and then fails only when the script calls `_experimental_fork(...)`. The script currently creates the parent with Daytona's default snapshot/class unless `DAYTONA_BASE_SNAPSHOT` is set.
- Files created/modified: `SUMMARY.md`.
- Commands run: searched `scripts/daytona-live-demo.js` and the installed `@daytona/sdk` package for fork/snapshot/runner references; inspected `scripts/daytona-live-demo.js` around `forkSandbox`, capability-blocker handling, and parent creation; inspected `@daytona/sdk/esm/Daytona.d.ts` for `create(...)` and `_experimental_fork(...)` signatures; inspected `@daytona/sdk/esm/Sandbox.d.ts` metadata fields; inspected `logs/daytona-live-run.txt`, `logs/daytona-live-latency.json`, and selected fields from `data/forking-console-state.json`; attempted a one-line metadata fetch for the parent sandbox, which failed due shell quoting before any useful API call was made.
- Test results: local evidence shows `create parent sandbox` and `prepare parent state` succeeded, while `fork frontend-hypothesis` failed with Daytona's server-side message `Forking is not supported for this sandbox`. SDK type definitions confirm fork is exposed by the installed SDK, and the demo script uses that API when available.
- Screenshots captured: none.
- Remaining TODOs: use a fork-capable Daytona snapshot/runner and set `DAYTONA_BASE_SNAPSHOT`, or ask Daytona/admin support which sandbox class/target in this organization supports `_experimental_fork`.
- Assumptions/blockers: exact parent sandbox class metadata was not fetched in this investigation. The observed error is still sufficient to distinguish permissions/auth from sandbox capability.

## Milestone 40: Found fork-capable Daytona path and completed live fork tree

- What changed: added `scripts/daytona-fork-capability-probe.js` plus `npm run daytona:inventory` and `npm run daytona:probe-fork`. The probe found that the default `us` container snapshot cannot fork, but target `us-east-1` with base snapshot `daytona-vm-small` can fork. Updated `scripts/daytona-live-demo.js` to use that fork-capable path by default, while allowing env overrides and `DAYTONA_USE_DEFAULT_SANDBOX=1` for the old default. Fixed cleanup handling so `DAYTONA_CLEANUP=1` is evaluated after env files load. Updated docs, `.env.daytona.example`, and generated state to reflect the successful live path.
- Files created/modified: `scripts/daytona-fork-capability-probe.js`, `scripts/daytona-live-demo.js`, `.env.daytona.example`, `package.json`, `../package.json`, `README.md`, `../README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `data/forking-console-state.json`, `logs/daytona-fork-capability-probe.json`, `logs/daytona-live-latency.json`, `logs/daytona-live-run.txt`, `dist/client/*`, `dist/build-info.json`, `SUMMARY.md`.
- Commands run: inspected Daytona SDK and generated API client type/runtime files for snapshots, runners, regions, sandbox classes, `create(...)`, and `_experimental_fork(...)`; `node --check scripts/daytona-fork-capability-probe.js`; package JSON parse checks; `npm run daytona:inventory` with network approval; inspected snapshot/region/sandbox inventory; `npm run daytona:probe-fork -- --targets=us-east-1 --snapshots=daytona-vm-small,daytona-vm-medium,daytona-vm,daytona-vm-large,daytona-vm-ubuntu-xxl --max-attempts=5` with network approval; patched live defaults; `npm run daytona:check`; `DAYTONA_CLEANUP=1 npm run demo:codex:live -- --default` with network approval; inspected live latency JSON, live run log, fork console state, and fork probe winner; patched stale generated state fields; stale-doc scans; `node --check` for updated Daytona scripts; JSON parse checks for state/log reports; root `npm run build`; root `npm run test`; root `npm run daytona:check`.
- Test results: `daytona:inventory` found 20 snapshots: 12 container, 3 windows, and 5 `linux-vm`; runner listing was unavailable to this API key (`404` public runners, `403` admin runners), but snapshot inventory was enough. `daytona:probe-fork` found `target=us-east-1` and `snapshot=daytona-vm-small` works: parent create `1526ms`, marker write `386ms`, fork `661ms`, inherited marker verification `157ms`; probe cleanup deleted its sandboxes. Full live demo with cleanup completed: parent create `1.478s`, prepare parent state `0.847s`, hypothesis forks `0.435s`/`0.637s`/`0.952s`, fork-a-fork refinements `0.748s` and `0.611s`, validator forks all passed, memory probe observed warmed process state, snapshot `coupon-prepared-baseline` created in `5.108s`, and cleanup deleted 10 sandboxes. Build passed. `npm run test` still fails in the expected three baseline coupon-bug assertions.
- Screenshots captured: none.
- Remaining TODOs: for the actual recording, run `npm run demo:codex:live` without `DAYTONA_CLEANUP=1` so Daytona keeps the fork tree visible in the console; after recording, clean up the created sandboxes manually or rerun with cleanup enabled for rehearsals. Capture terminal/console screenshots if the live path is used in the post.
- Assumptions/blockers: live fork now works only through the discovered `linux-vm` path. Daytona runner listing is still not available to this API key, so the script relies on the discovered snapshot/target instead of enumerating runner internals. The memory probe is reported as observed behavior for this runner, not a universal guarantee for every Daytona backend.

## Milestone 41: Added linux-vm snapshot fallback after transient VM start failure

- What changed: after a user live run failed at parent creation with `Sandbox failed to start: internal error` on `daytona-vm-small`, re-ran the fork probe and found `daytona-vm-medium` was currently healthy and fork-capable. Updated the live runner to use a fallback list of known `linux-vm` snapshots instead of a single base snapshot. The default order now starts with `daytona-vm-medium`, then falls back to `daytona-vm-small`, `daytona-vm`, `daytona-vm-large`, and `daytona-vm-ubuntu-xxl`. Updated docs and `.env.daytona.example` to describe the fallback behavior.
- Files created/modified: `scripts/daytona-live-demo.js`, `.env.daytona.example`, `README.md`, `../README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `data/forking-console-state.json`, `logs/daytona-fork-capability-probe.json`, `logs/daytona-live-latency.json`, `logs/daytona-live-run.txt`, `dist/client/*`, `dist/build-info.json`, `SUMMARY.md`.
- Commands run: inspected the failed live latency/log output; inspected current `linux-vm` snapshots from `logs/daytona-fork-capability-probe.json`; inspected `scripts/daytona-live-demo.js`; `npm run daytona:probe-fork -- --targets=us-east-1 --snapshots=daytona-vm-small,daytona-vm-medium,daytona-vm,daytona-vm-large,daytona-vm-ubuntu-xxl --max-attempts=6` with network approval; patched snapshot fallback logic; updated docs; `node --check scripts/daytona-live-demo.js`; `npm run daytona:check`; package JSON parse checks; `DAYTONA_CLEANUP=1 npm run demo:codex:live -- --default` with network approval; inspected live latency/state reports; stale wording scans; JSON parse checks; root `npm run build`; root `npm run test`.
- Test results: user run failure was isolated to parent create/start on `daytona-vm-small` after `593ms`, before any fork operation. Probe then showed default snapshot unavailable in `us-east-1`, `daytona-vm-small` failing with internal error, and `daytona-vm-medium` succeeding: parent create `1193ms`, marker write `358ms`, fork `464ms`, inherited marker verification `135ms`. The updated fallback live run completed end to end with `daytona-vm-medium`: parent create `1.695s`, prepare parent `0.879s`, hypothesis forks `0.633s`/`1.091s`/`1.433s`, fork-a-fork refinements `1.146s` and `0.830s`, memory probe observed warmed process state, snapshot `5.415s`, and cleanup deleted 10 sandboxes. Build passed. `npm run test` still fails in the expected three baseline coupon-bug assertions.
- Screenshots captured: none.
- Remaining TODOs: for the recording, run `npm run demo:codex:live` without `DAYTONA_CLEANUP=1`; if a first snapshot has a transient start issue, the command should now continue to the next `linux-vm` snapshot automatically and print the failed/successful create rows.
- Assumptions/blockers: `daytona-vm-small` appears intermittently unhealthy, so the demo should not rely on a single snapshot. The fallback list still assumes `us-east-1` remains available for the organization.

## Milestone 42: Added narrated live-run explanation breaks

- What changed: added `[explain]` blocks after each major live Daytona phase: prepared parent captured, hypothesis forks, fork-a-fork refinements, validator fan-out, runtime-state probe, snapshot baseline, and final summary. The blocks include concise agent reasoning about why the next fork point matters. Interactive `npm run demo:codex:live` runs now pass `--pause-steps` to the live runner, so the terminal pauses after each explanation for narration; `--default` and `--no-step-pause` keep rehearsal/automation runs noninteractive.
- Files created/modified: `scripts/daytona-live-demo.js`, `scripts/codex-fork-demo.js`, `README.md`, `../README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `data/forking-console-state.json`, `logs/daytona-live-latency.json`, `logs/daytona-live-run.txt`, `logs/codex-fork-session.md`, `logs/codex-fork-session.json`, `logs/simple-demo-run.json`, `logs/simple-demo-latency-report.md`, `fix-candidate.md`, `dist/client/*`, `dist/build-info.json`, `SUMMARY.md`.
- Commands run: `node --check daytona-forking-coupon-demo/scripts/daytona-live-demo.js`; `node --check daytona-forking-coupon-demo/scripts/codex-fork-demo.js`; `rg` checks for explanation/pause wiring; `npm run daytona:check`; `npm run build`; `npm run demo:codex -- --default`; `npm run test`; cleanup-enabled live Daytona rehearsal `DAYTONA_CLEANUP=1 npm run demo:codex:live -- --default` with network approval; inspected `logs/daytona-live-latency.json` with a corrected `node -e` command after one failed shell-quoting attempt.
- Test results: syntax checks passed. `daytona:check` reports `DAYTONA_API_KEY` configured, default target `us-east-1`, and fallback snapshots starting with `daytona-vm-medium`. Build passed. `demo:codex -- --default` still prints the deterministic one-shot transcript and writes its session artifacts. `npm run test` still fails in the expected three baseline coupon-bug assertions. The live rehearsal completed with explanation blocks and deleted its 10 created sandboxes: parent create `1.421s`, prepare parent `0.718s`, fork latencies `0.495s`, `1.128s`, `0.844s`, `1.061s`, `0.638s`, `0.549s`, `0.641s`, `0.679s`, and `0.551s`; snapshot creation took `5.244s`; memory-state probe observed warmed process state for this runner.
- Screenshots captured: none.
- Remaining TODOs: for the recorded take, run `npm run demo:codex:live` without `DAYTONA_CLEANUP=1` so the terminal pauses for narration and Daytona keeps the fork tree visible. Capture a screenshot or screen recording of at least one `[explain]` block if the post needs a static artifact.
- Assumptions/blockers: the explanation text is scripted demo reasoning, not hidden model chain-of-thought. Runtime memory-state wording remains conservative and runner-specific.

## Milestone 43: Cleaned recording output and simplified browser hypothesis view

- What changed: removed recording tips/artifact paths from normal live terminal output, removed the final useless continue prompt, and made `--verbose-artifacts` the only path that prints local artifact file paths at the end. Added browser fixed-state behavior: a successful live run now marks `data/demo-session-state.json` with `fixApplied: true`, and the open checkout page polls that state so `SUMMER-2026` changes to `Invalid coupon code` after the terminal demo completes. Simplified the root webpage from terminal/operator panes into three clean hypothesis cards; `/forking-demo` remains the dense reference page. Added robust late-failure cleanup so `DAYTONA_CLEANUP=1` deletes tracked sandboxes even if a later validator fork fails.
- Files created/modified: `scripts/daytona-live-demo.js`, `scripts/codex-fork-demo.js`, `src/client/index.html`, `src/client/app.js`, `src/client/forking-demo.js`, `src/client/forking-demo.css`, `src/client/styles.css`, `src/server/app.js`, `scripts/video-reset.js`, `scripts/simple-demo.js`, `README.md`, `../README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `logs/daytona-live-run.txt`, `logs/daytona-live-latency.json`, `logs/codex-fork-session.md`, `logs/codex-fork-session.json`, `logs/simple-demo-run.json`, `logs/simple-demo-latency-report.md`, `fix-candidate.md`, `dist/client/*`, `dist/build-info.json`, `SUMMARY.md`.
- Commands run: source inspections with `rg` and `sed`; syntax checks for `scripts/daytona-live-demo.js`, `scripts/codex-fork-demo.js`, `src/client/app.js`, and `src/client/forking-demo.js`; `npm run build`; `npm run demo:codex -- --default`; stale recording-text scans for `Recording tip`, `Latency artifacts`, old operator-console headings, and `Sandboxes kept so`; `npm run test`; attempted cleanup-enabled live rehearsal `DAYTONA_CLEANUP=1 npm run demo:codex:live -- --default`; inspected `logs/daytona-live-run.txt`, `logs/daytona-live-latency.json`, and `data/forking-console-state.json`; deleted seven partial rehearsal sandboxes by ID with the Daytona SDK; `npm run daytona:check`; inspected `data/demo-session-state.json`.
- Test results: syntax checks passed. Build passed and regenerated `dist/client`. `demo:codex -- --default` still prints the deterministic transcript and writes session artifacts. `npm run test` still fails in the expected three baseline coupon-bug assertions. The cleanup rehearsal verified the cleaned header output but hit Daytona's organization CPU limit during `fork implementation-review`; before the cleanup fix was added, that partial run left seven sandboxes, which were then deleted manually. Final `daytona:check` passes, and `data/demo-session-state.json` currently has `bugReproduced: true` and `fixApplied: false`, so the next recording can still start from the broken UI.
- Screenshots captured: none.
- Remaining TODOs: before another full recorded live run, delete any older intentionally kept Daytona demo sandboxes if CPU capacity is near the limit. Then run `npm run demo:codex:live` without `DAYTONA_CLEANUP=1` for the take; the browser should flip to the fixed inline-validation state after the run completes.
- Assumptions/blockers: the failed rehearsal was a Daytona resource-limit issue, not a local code path problem. The browser fixed-state is a recording affordance triggered by the completed live demo state; the baseline source tests intentionally remain red until the actual shared-contract code is changed in a fork.

## Milestone 44: Removed biased hypothesis labels from root UI

- What changed: made the root-page hypothesis cards neutral. Removed the `Likely best`, `Chosen`, `Partial`, `winning branch`, and "This fixes the invariant" language from the recording-facing UI. All three cards now display as `hypothesis` with `Hypothesis to test` and question-style reasoning; the terminal remains responsible for deciding which branch wins.
- Files created/modified: `src/client/forking-demo.js`, `dist/client/forking-demo.js`, `dist/build-info.json`, `SUMMARY.md`.
- Commands run: inspected root hypothesis rendering with `rg` and `sed`; `node --check daytona-forking-coupon-demo/src/client/forking-demo.js`; `npm run build`; stale-label scan for `Likely best`, `Chosen`, `winning branch`, `This fixes the invariant`, and `Partial`; confirmation scan for `Hypothesis to test`; `npm run test`.
- Test results: syntax check passed. Build passed and regenerated `dist/client`. Stale-label scan found no biased labels in the recording-facing source or built files. `npm run test` still fails in the expected three baseline coupon-bug assertions.
- Screenshots captured: none.
- Remaining TODOs: hard refresh the browser or restart `npm run dev` before recording so the root page serves the rebuilt neutral cards.
- Assumptions/blockers: the dense `/forking-demo` reference page and terminal scripts can still describe the eventual winning branch; this change only removes pre-decision bias from the root recording UI.

## Milestone 45: Moved bug explanation below checkout status

- What changed: moved the `Bug appears here` explanation block so it appears below `Checkout has not started.` in the root checkout card. This keeps the explanatory copy visually attached to the checkout result instead of interrupting the form and totals.
- Files created/modified: `src/client/index.html`, `dist/client/index.html`, `dist/build-info.json`, `SUMMARY.md`.
- Commands run: patched `src/client/index.html`; `npm run build`; `node --check daytona-forking-coupon-demo/src/client/app.js`; inspected source and built HTML around the checkout message.
- Test results: build passed and regenerated `dist/client`. `app.js` syntax check passed. Source and built HTML both show `.bug-location` directly after `#checkout-message`.
- Screenshots captured: none.
- Remaining TODOs: hard refresh the browser before recording so the checkout card picks up the rebuilt layout.
- Assumptions/blockers: no functional coupon behavior changed; this was a layout-only move.

## Milestone 46: Removed duplicate checkout error text from static bug note

- What changed: changed the static `Bug appears here` note from `After Checkout: Checkout failed. Please try another payment method.` to `After Checkout: generic checkout error shown above`. The full checkout error now appears on screen only in the live checkout status message after the user clicks Checkout.
- Files created/modified: `src/client/index.html`, `dist/client/index.html`, `dist/build-info.json`, `SUMMARY.md`.
- Commands run: patched `src/client/index.html`; `npm run build`; `node --check daytona-forking-coupon-demo/src/client/app.js`; scanned source and built client/server files for the exact checkout error text and the new static note; inspected source and built HTML around the checkout message.
- Test results: build passed and regenerated `dist/client`. `app.js` syntax check passed. Static root HTML now uses `generic checkout error shown above`; the exact checkout error remains only in runtime API/client logic.
- Screenshots captured: none.
- Remaining TODOs: hard refresh the browser before recording so the checkout card picks up the rebuilt copy.
- Assumptions/blockers: no functional coupon behavior changed; this only removes duplicate visible copy from the static explanatory block.

## Milestone 47: Reveal bug note only after reproduction

- What changed: made the `Bug appears here` note hidden on first load and revealed it only after the invalid coupon is accepted and checkout fails generically. If `data/demo-session-state.json` already records the reproduction, the page reveals the note on poll/reload. The fixed-state path also reveals the same area before changing it to the green fixed behavior.
- Files created/modified: `src/client/index.html`, `src/client/app.js`, `src/client/styles.css`, `dist/client/index.html`, `dist/client/app.js`, `dist/client/styles.css`, `dist/build-info.json`, `SUMMARY.md`.
- Commands run: patched `src/client/index.html`, `src/client/app.js`, and `src/client/styles.css`; `node --check daytona-forking-coupon-demo/src/client/app.js`; `npm run build`; scanned source and built files for `bug-location`, `showBugLocation`, `bugReproduced`, and the static checkout note; inspected source and built HTML around the checkout message.
- Test results: `app.js` syntax check passed. Build passed and regenerated `dist/client`. Source and built HTML now render `<div class="bug-location" hidden>`, with `.bug-location[hidden] { display: none; }`, and the script reveals it from the reproduction/session state.
- Screenshots captured: none.
- Remaining TODOs: hard refresh the browser before recording. For a fresh take, run `npm run demo:reset` or reset the session so the bug note starts hidden.
- Assumptions/blockers: this preserves the persisted-session behavior: if the bug was already reproduced before page load, the note appears because the bug has already been made.

## Milestone 48: Shortened default live Daytona run to avoid CPU cap

- What changed: changed the default `npm run demo:codex:live` path so it creates the parent sandbox, three hypothesis forks, and two contract refinements, then completes before validator fan-out. This keeps the recording path under the Daytona CPU limit while still showing the key multi-level forking story. Added `--full-tree` as the opt-in path for the older validator/memory/snapshot expansion. Updated reset and missing-marker messages to point to `npm run demo:codex:live`.
- Files created/modified: `scripts/daytona-live-demo.js`, `scripts/codex-fork-demo.js`, `scripts/simple-demo.js`, `README.md`, `../README.md`, `DEMO.md`, `logs/daytona-live-latency.json`, `logs/daytona-live-run.txt`, `data/demo-session-state.json`, `data/forking-console-state.json`, `SUMMARY.md`.
- Commands run: patched the live runner, Codex wrapper, reset script, and docs; syntax checks for `scripts/daytona-live-demo.js`, `scripts/codex-fork-demo.js`, and `scripts/simple-demo.js`; deleted six sandboxes from the failed user run by ID; ran cleanup-enabled shortened rehearsal `DAYTONA_CLEANUP=1 npm run demo:codex:live -- --default`; ran `npm run demo:reset`; ran `npm run daytona:check`; ran `npm run test`; inspected `data/demo-session-state.json` and `logs/daytona-live-latency.json`.
- Test results: syntax checks passed. The shortened cleanup rehearsal completed successfully and deleted its six sandboxes: parent create `1.398s`, prepare parent `0.661s`, hypothesis fork latencies `0.433s`, `0.620s`, `1.145s`, contract refinement fork latencies `1.018s` and `0.784s`, total fork latency `4.000s`. `daytona:check` passed. `npm run test` still fails in the expected three baseline coupon-bug assertions. After reset, `data/demo-session-state.json` has `bugReproduced: false` and `fixApplied: false`.
- Screenshots captured: none.
- Remaining TODOs: for the next recording take, hard refresh the browser, reproduce the bug, then run `npm run demo:codex:live`. Use `npm run demo:codex:live -- --full-tree` only when enough Daytona CPU capacity is available.
- Assumptions/blockers: the CPU-limit failure came from creating validator sandboxes after six VM sandboxes were already active. The default recording path now avoids that fan-out; validator/memory/snapshot semantics remain available as documented or with `--full-tree`.

## Milestone 49: Prepared guide screenshot set and ChatGPT drafting brief

- What changed: created guide-specific screenshot assets from the latest live demo logs, added a concise screenshot recommendation document, and added a paste-ready ChatGPT brief for rewriting the forking guide around the current coupon demo. Updated the screenshot manifest with the new guide images and their source pages.
- Files created/modified: `scripts/render-guide-assets.js`, `docs/guide-screenshot-set.md`, `docs/guide-chatgpt-brief.md`, `docs/screenshots/manifest.md`, `docs/screenshots/pages/14-guide-live-fork-run.html`, `docs/screenshots/pages/15-guide-live-latency.html`, `docs/screenshots/pages/16-guide-current-fork-tree.html`, `docs/screenshots/pages/17-guide-state-semantics.html`, `docs/screenshots/14-guide-live-fork-run.png`, `docs/screenshots/15-guide-live-latency.png`, `docs/screenshots/16-guide-current-fork-tree.png`, `docs/screenshots/17-guide-state-semantics.png`, `SUMMARY.md`.
- Commands run: inspected existing screenshots and manifest; wrote `scripts/render-guide-assets.js`; `node --check scripts/render-guide-assets.js`; `node scripts/render-guide-assets.js`; rendered guide HTML pages with `qlmanage -t -s 1280 -o docs/screenshots`; renamed rendered `.html.png` outputs to final guide screenshot filenames; visually inspected `14-guide-live-fork-run.png`, `15-guide-live-latency.png`, `16-guide-current-fork-tree.png`, and `17-guide-state-semantics.png`; re-rendered after layout fixes; `file docs/screenshots/14-guide-live-fork-run.png docs/screenshots/15-guide-live-latency.png docs/screenshots/16-guide-current-fork-tree.png docs/screenshots/17-guide-state-semantics.png`; `rg` checks for new guide screenshot references.
- Test results: `scripts/render-guide-assets.js` passed syntax check. The render script generated all four guide HTML pages. All four PNGs are valid 1280 x 1280 images. Visual inspection confirmed the live terminal, latency table, current fork tree, and state-semantics visuals are usable for the guide.
- Screenshots captured: `docs/screenshots/14-guide-live-fork-run.png`, `docs/screenshots/15-guide-live-latency.png`, `docs/screenshots/16-guide-current-fork-tree.png`, and `docs/screenshots/17-guide-state-semantics.png`.
- Remaining TODOs: attach the recommended 4-5 screenshots to the guide draft and use `docs/guide-chatgpt-brief.md` as the drafting prompt. Optionally swap in `15-guide-live-latency.png` when the guide needs concrete timing data over lifecycle semantics.
- Assumptions/blockers: the guide numbers come from the latest successful live run and should be described as measured demo observations, not general Daytona performance claims. The recommended short screenshot set intentionally avoids older validator/checker-heavy visuals.

## Milestone 50: Packaged final guide image inserts

- What changed: created a dedicated `docs/screenshots/guide-inserts/` set with five guide-ready screenshots named for the exact sections in the final guide draft. Added `docs/guide-image-inserts.md` with paste-ready markdown, alt text, captions, and placement rationale for each `[SCREENSHOT: ...]` placeholder. Updated the guide screenshot set and screenshot manifest to point at the stable insert filenames.
- Files created/modified: `docs/screenshots/guide-inserts/01-demo-bug.png`, `docs/screenshots/guide-inserts/02-prepared-parent-state.png`, `docs/screenshots/guide-inserts/03-live-fork-run.png`, `docs/screenshots/guide-inserts/04-multilevel-fork-tree.png`, `docs/screenshots/guide-inserts/05-state-semantics.png`, `docs/guide-image-inserts.md`, `docs/guide-screenshot-set.md`, `docs/screenshots/manifest.md`, `SUMMARY.md`.
- Commands run: `mkdir -p docs/screenshots/guide-inserts`; copied source screenshots into guide insert filenames; inspected source screenshot HTML for the broken coupon UI and prepared parent checklist; `file docs/screenshots/guide-inserts/*.png`; `sed -n` inspection of `docs/guide-image-inserts.md`; `rg` checks for all guide insert paths across `docs/`.
- Test results: all five guide insert files are valid 1280 x 1280 PNGs. The insert markdown references all five files correctly, and the screenshot manifest now includes each guide insert.
- Screenshots captured: no new browser capture was needed; guide-ready copies were prepared from the already generated screenshot assets.
- Remaining TODOs: replace the five screenshot placeholders in the guide draft with the markdown snippets from `docs/guide-image-inserts.md`. Use `docs/screenshots/15-guide-live-latency.png` only if you want a timing-focused optional swap.
- Assumptions/blockers: the final guide draft uses five screenshot placeholders, so the package keeps the image set to five screenshots rather than adding the optional latency table as a sixth required image.

## Milestone 51: Regenerated guide inserts with current demo UI

- What changed: replaced the guide insert screenshots so they no longer reuse older static visuals. `scripts/render-guide-assets.js` now generates five current-UI guide pages that import the settled app CSS, show the current bug reproduction UI with neutral hypothesis cards, use current-style prepared-state and terminal panels, and present the multi-level fork tree as a cleaner level view that fits the square guide image. Updated the guide insert docs and screenshot manifest to describe the new generated source pages.
- Files created/modified: `scripts/render-guide-assets.js`, `docs/screenshots/pages/guide-01-demo-bug.html`, `docs/screenshots/pages/guide-02-prepared-parent-state.html`, `docs/screenshots/pages/guide-03-live-fork-run.html`, `docs/screenshots/pages/guide-04-multilevel-fork-tree.html`, `docs/screenshots/pages/guide-05-state-semantics.html`, `docs/screenshots/guide-inserts/01-demo-bug.png`, `docs/screenshots/guide-inserts/02-prepared-parent-state.png`, `docs/screenshots/guide-inserts/03-live-fork-run.png`, `docs/screenshots/guide-inserts/04-multilevel-fork-tree.png`, `docs/screenshots/guide-inserts/05-state-semantics.png`, `docs/guide-image-inserts.md`, `docs/guide-screenshot-set.md`, `docs/screenshots/manifest.md`, `SUMMARY.md`.
- Commands run: inspected current `src/client/index.html`, `src/client/styles.css`, `src/client/forking-demo.css`, `src/client/forking-demo.js`, `data/forking-console-state.json`, and `data/demo-session-state.json`; patched `scripts/render-guide-assets.js`; `node --check scripts/render-guide-assets.js`; `node scripts/render-guide-assets.js`; rendered current-UI guide pages with `qlmanage -t -s 1280 -o docs/screenshots/guide-inserts`; renamed generated `.html.png` outputs over the final guide insert filenames; visually inspected `01-demo-bug.png`, `02-prepared-parent-state.png`, `03-live-fork-run.png`, `04-multilevel-fork-tree.png`, and `05-state-semantics.png`; `file docs/screenshots/guide-inserts/*.png`; `rg` checks for source-page and current-UI documentation references.
- Test results: `scripts/render-guide-assets.js` passed syntax check. All five regenerated guide insert PNGs are valid 1280 x 1280 images. Visual inspection confirmed the screenshots now match the current simplified demo UI and no longer use the old standalone visual treatment.
- Screenshots captured: refreshed `docs/screenshots/guide-inserts/01-demo-bug.png`, `02-prepared-parent-state.png`, `03-live-fork-run.png`, `04-multilevel-fork-tree.png`, and `05-state-semantics.png` from current-UI source pages.
- Remaining TODOs: insert the five refreshed guide images into the final guide draft using `docs/guide-image-inserts.md`.
- Assumptions/blockers: the screenshots are static guide renders of the current UI state, not a fresh browser automation capture from localhost. This avoids relying on the local dev server or Playwright availability while keeping the visuals aligned with the settled app CSS.

## Milestone 52: Cleaned root UI copy for coding-agent forking story

- What changed: simplified the recording-facing root UI copy so it foregrounds the general coding-agent workflow instead of a step-by-step Daytona walkthrough. The hero now reads `Coding agents + sandbox forking` and `Prepare once. Fork the investigation.` The bug panel now frames the checkout failure as captured evidence written to disk, and the hypothesis section now reads `Forked agent paths` with `Same parent state, different debugging strategies`. Updated the guide screenshot renderer and reset helper wording to match.
- Files created/modified: `src/client/index.html`, `scripts/render-guide-assets.js`, `scripts/simple-demo.js`, `dist/client/index.html`, `dist/build-info.json`, `docs/screenshots/pages/guide-01-demo-bug.html`, `docs/screenshots/pages/guide-02-prepared-parent-state.html`, `docs/screenshots/pages/guide-03-live-fork-run.html`, `docs/screenshots/pages/guide-04-multilevel-fork-tree.html`, `docs/screenshots/pages/guide-05-state-semantics.html`, `docs/screenshots/pages/14-guide-live-fork-run.html`, `docs/screenshots/pages/15-guide-live-latency.html`, `docs/screenshots/pages/16-guide-current-fork-tree.html`, `docs/screenshots/pages/17-guide-state-semantics.html`, `docs/screenshots/guide-inserts/01-demo-bug.png`, `SUMMARY.md`.
- Commands run: searched for old UI copy with `rg`; inspected `src/client/index.html`, `src/client/styles.css`, and `scripts/render-guide-assets.js`; patched the root HTML, guide screenshot renderer, and reset helper; `node --check scripts/render-guide-assets.js`; `node --check scripts/simple-demo.js`; `npm run build`; `node scripts/render-guide-assets.js`; rendered `docs/screenshots/pages/guide-01-demo-bug.html` with `qlmanage -t -s 1280`; moved the rendered PNG over `docs/screenshots/guide-inserts/01-demo-bug.png`; visually inspected the refreshed bug screenshot; ran `rg` checks to confirm new copy is present in source, dist, and guide page and old root-page phrases are absent from the relevant files; checked the refreshed PNG with `file`.
- Test results: syntax checks passed. `npm run build` passed and regenerated `dist/client`. The refreshed `01-demo-bug.png` is a valid 1280 x 1280 PNG and visually shows the cleaner coding-agent/forking copy.
- Screenshots captured: refreshed `docs/screenshots/guide-inserts/01-demo-bug.png` with the cleaned root UI copy.
- Remaining TODOs: hard refresh the local browser tab while `npm run dev` is running so the new root copy is visible during recording.
- Assumptions/blockers: this was a copy and static-render update; no app behavior or coupon validation logic changed.

## Milestone 53: Enabled live Daytona sandbox cleanup by default

- What changed: flipped the live Daytona demo so created sandboxes are cleaned up by default after the run completes or fails. Added explicit keep opt-outs: `npm run demo:codex:live -- --keep-sandboxes`, `--no-cleanup`, or `DAYTONA_KEEP_SANDBOXES=1`. Updated the Codex wrapper to pass cleanup/keep flags through correctly and to avoid treating those flags as the user prompt. Updated docs and Daytona env files so cleanup is no longer disabled by local configuration.
- Files created/modified: `scripts/daytona-live-demo.js`, `scripts/codex-fork-demo.js`, `.env.daytona.example`, `.env.daytona.local`, `README.md`, `../README.md`, `DEMO.md`, `docs/screenshot-checklist.md`, `SUMMARY.md`.
- Commands run: searched cleanup behavior and docs with `rg`; inspected `scripts/daytona-live-demo.js`, `scripts/codex-fork-demo.js`, README/DEMO cleanup sections, and Daytona env cleanup settings; patched live cleanup logic, wrapper flag passthrough, env cleanup defaults, and docs; `node --check scripts/daytona-live-demo.js`; `node --check scripts/codex-fork-demo.js`; `npm run daytona:check`; repeated stale cleanup wording scans with `rg`; inspected recently modified files with `find`.
- Test results: syntax checks passed. `npm run daytona:check` now reports `Cleanup: enabled by default`, with the existing configured Daytona key and fork-capable default target/snapshot list. Stale scans found no remaining active docs that recommend `DAYTONA_CLEANUP=1` or cleanup-disabled runs as the default path.
- Screenshots captured: none.
- Remaining TODOs: run `npm run demo:codex:live` for the next real take; it will delete tracked Daytona sandboxes at the end. Use `-- --keep-sandboxes` only for a deliberate Daytona-console screenshot where IDs need to remain visible after exit.
- Assumptions/blockers: I did not run a full live Daytona fork session in this turn to avoid creating billable/limited resources just to test cleanup. The cleanup path had already been exercised by prior rehearsals; this change makes that path the default and verifies it with `daytona:check`.

## Milestone 54: Added live OpenAI prompt-cache metrics to fork demo

- What changed: added `scripts/openai-cache-probe.js`, a live OpenAI Responses API cache probe that prints `[cache:start]` and `[cache:ok]` rows with actual input tokens, cached tokens, output tokens, and latency. Integrated it into `npm run demo:codex:live` at the prepared-parent fork layer and the contract-branch refinement layer. Added `npm run demo:cache` for standalone rehearsal. Updated docs and help text to distinguish OpenAI prompt-cache hits from Daytona sandbox state forking.
- Files created/modified: `scripts/openai-cache-probe.js`, `scripts/daytona-live-demo.js`, `scripts/codex-fork-demo.js`, `scripts/simple-demo.js`, `package.json`, `../package.json`, `README.md`, `../README.md`, `DEMO.md`, `logs/openai-cache-probe.json`, `logs/openai-cache-probe.md`, `dist/client/*`, `SUMMARY.md`.
- Commands run: inspected OpenAI API-key and docs skills; safely checked for an existing `OPENAI_API_KEY` without printing it; inspected `scripts/codex-fork-demo.js`, `scripts/daytona-live-demo.js`, `scripts/agent-reasoning.js`, package files, README, DEMO, and helper text; patched the new cache probe and live-run integration; `node --check scripts/openai-cache-probe.js`; `node --check scripts/daytona-live-demo.js`; `node --check scripts/codex-fork-demo.js`; `node --check scripts/simple-demo.js`; package JSON parse checks; `npm run daytona:check`; `npm run build`; `npm run demo:help`; live OpenAI cache probes with `npm run demo:cache -- --layer1-only` and `npm run demo:cache`; JSON summary inspection of `logs/openai-cache-probe.json`; markdown inspection of `logs/openai-cache-probe.md`; attempted `git status --short` from the workspace root, which failed because the current directory is not a Git repository.
- Test results: syntax checks passed. `npm run build` passed and regenerated `dist/client`. `npm run daytona:check` reports `OpenAI cache probe: configured` without printing secrets. The live cache probe succeeded with `gpt-4.1-mini`: layer 1 child prompts reused `14,976` of `15,704` input tokens (`95%`), and layer 2 refinement prompts reused `7,424` of `7,871` input tokens (`94%`). `logs/openai-cache-probe.json` parses and contains seven rows across `layer 1` and `layer 2`.
- Screenshots captured: none.
- Remaining TODOs: run the full `npm run demo:codex:live` recording path after reproducing the browser bug; it will now show live Daytona fork timings and live OpenAI cached-token rows in one terminal sequence. Capture a new terminal screenshot if this metric becomes part of the guide image set.
- Assumptions/blockers: the OpenAI cache rows are live usage metadata from identical serialized prompt prefixes; they should be described as model-side prompt caching, not as Daytona copying OpenAI KV cache between sandboxes. I did not run a full Daytona fork session in this milestone because the standalone cache probe was sufficient to verify the new OpenAI metrics without creating Daytona sandboxes.

## Milestone 55: Configured live demo sandboxes to auto-stop after 15 minutes

- What changed: changed the live Daytona demo parent sandbox creation from `autoStopInterval: 0` to a configurable 15-minute auto-stop. Added `setAutostopInterval(15)` immediately after each fork is created, while tracking the fork first so cleanup can still delete it if the setter fails. Applied the same 15-minute guard to the Daytona fork capability probe. Added `DAYTONA_DEMO_AUTOSTOP_MINUTES=15` to Daytona env files and documented the setting as a backup guard in addition to cleanup-at-exit.
- Files created/modified: `scripts/daytona-live-demo.js`, `scripts/daytona-fork-capability-probe.js`, `.env.daytona.example`, `.env.daytona.local`, `README.md`, `../README.md`, `DEMO.md`, `SUMMARY.md`.
- Commands run: searched for existing auto-stop and fork calls with `rg`; inspected installed `@daytona/sdk` type/runtime files to confirm `autoStopInterval` is minutes and `setAutostopInterval()` is the child-sandbox setter; patched the live runner, capability probe, env files, and docs; `node --check scripts/daytona-live-demo.js`; `node --check scripts/daytona-fork-capability-probe.js`; `npm run daytona:check`; scanned updated files for auto-stop references.
- Test results: syntax checks passed. `npm run daytona:check` now reports `Sandbox auto-stop: 15 minutes` with cleanup still enabled by default and OpenAI cache probe configured. The installed SDK confirms `autoStopInterval` uses minutes and `0` disables auto-stop.
- Screenshots captured: none.
- Remaining TODOs: run the next real `npm run demo:codex:live` recording take after reproducing the browser bug; any parent/fork sandboxes created by that run will now have the 15-minute inactivity auto-stop guard even if the command is interrupted or `--keep-sandboxes` is used.
- Assumptions/blockers: I did not create live Daytona sandboxes for this small config change; verification was by SDK inspection, syntax checks, and `daytona:check`. Cleanup remains the primary end-of-run cleanup path, while auto-stop protects interrupted or intentionally kept runs.

## Milestone 56: Restyled demo UI and guide assets to Daytona dark mode

- What changed: restyled the recording-facing checkout UI, forking visual reference, generated graph SVGs, and guide insert screenshots with a Daytona docs-inspired dark palette: black page background, charcoal panels, gray borders, white/gray text, and green highlights. Kept the root page focused on the coupon bug plus neutral hypothesis cards while leaving advanced panels hidden on the recording page. Updated guide image docs and screenshot manifest to describe the refreshed dark-mode assets.
- Files created/modified: `src/client/styles.css`, `src/client/forking-demo.css`, `scripts/render-guide-assets.js`, `scripts/render-graphs.js`, `docs/graphs/state-layers.svg`, `docs/graphs/lifecycle-comparison.svg`, `docs/graphs/fork-tree.svg`, `docs/graphs/state-reuse-benchmark.svg`, `docs/graphs/branch-outcomes.svg`, `docs/graphs/index.html`, `docs/screenshots/pages/guide-01-demo-bug.html`, `docs/screenshots/pages/guide-02-prepared-parent-state.html`, `docs/screenshots/pages/guide-03-live-fork-run.html`, `docs/screenshots/pages/guide-04-multilevel-fork-tree.html`, `docs/screenshots/pages/guide-05-state-semantics.html`, `docs/screenshots/pages/14-guide-live-fork-run.html`, `docs/screenshots/pages/15-guide-live-latency.html`, `docs/screenshots/pages/16-guide-current-fork-tree.html`, `docs/screenshots/pages/17-guide-state-semantics.html`, `docs/screenshots/guide-inserts/01-demo-bug.png`, `docs/screenshots/guide-inserts/02-prepared-parent-state.png`, `docs/screenshots/guide-inserts/03-live-fork-run.png`, `docs/screenshots/guide-inserts/04-multilevel-fork-tree.png`, `docs/screenshots/guide-inserts/05-state-semantics.png`, `docs/screenshots/manifest.md`, `docs/guide-screenshot-set.md`, `docs/guide-image-inserts.md`, `dist/client/index.html`, `dist/client/styles.css`, `dist/client/forking-demo.css`, `dist/build-info.json`, `SUMMARY.md`.
- Commands run: fetched and inspected the referenced Daytona docs page/CSS for palette cues; attempted `git status --short` from the workspace root, which failed because the current directory is not a Git repository; inspected source HTML/CSS/JS with `rg` and `sed`; patched app CSS, guide renderer, graph renderer, screenshot docs, and manifest; `npm run build`; `node --check daytona-forking-coupon-demo/scripts/render-guide-assets.js`; `node --check daytona-forking-coupon-demo/scripts/render-graphs.js`; `npm --prefix daytona-forking-coupon-demo run render-graphs`; `node daytona-forking-coupon-demo/scripts/render-guide-assets.js`; rendered guide insert pages with `qlmanage -t -s 1280 -o daytona-forking-coupon-demo/docs/screenshots/guide-inserts`; moved generated `.html.png` files over the stable guide insert names; visually inspected `01-demo-bug.png` and `05-state-semantics.png`; checked PNG dimensions with `file`; scanned for stale light-theme tokens and recording-page probe labels.
- Test results: `npm run build` passed and regenerated `dist/client`. Both render scripts passed syntax checks. `npm --prefix daytona-forking-coupon-demo run render-graphs` regenerated all graph SVGs successfully. `node scripts/render-guide-assets.js` regenerated all guide HTML pages successfully. All five guide insert PNGs are valid 1280 x 1280 images. Visual inspection confirmed the checkout bug screenshot and lifecycle/state screenshot now use the dark Daytona-style palette with green highlights.
- Screenshots captured: refreshed `docs/screenshots/guide-inserts/01-demo-bug.png`, `02-prepared-parent-state.png`, `03-live-fork-run.png`, `04-multilevel-fork-tree.png`, and `05-state-semantics.png`.
- Remaining TODOs: hard refresh the local browser tab or restart `npm run dev` before recording so the rebuilt dark UI is visible. If the guide uses any older numbered screenshots outside `docs/screenshots/guide-inserts/`, regenerate those separately if they need to match the new theme.
- Assumptions/blockers: the styling follows the referenced Daytona docs dark-mode direction without importing Daytona site fonts or remote assets. The root recording page already hides the old mode/probe/status sections via `.unified-console`; the detailed `/forking-demo` reference page still keeps advanced inspection panels for debugging.

## Milestone 57: Added Svelte blog components for screenshots 4 and 5

- What changed: created portable Svelte components that replace the small-text PNGs for the multi-level fork tree and lifecycle/state semantics sections. The components use responsive HTML cards, real text, article-width layouts, and the Daytona dark-mode palette so the blog can render them directly instead of embedding `04-multilevel-fork-tree.png` and `05-state-semantics.png`. Updated guide helper docs to recommend the components first and keep PNGs as fallbacks.
- Files created/modified: `docs/blog-components/svelte/MultiLevelForkTree.svelte`, `docs/blog-components/svelte/LifecycleStateSemantics.svelte`, `docs/blog-components/svelte/index.js`, `docs/blog-components/svelte/README.md`, `docs/guide-image-inserts.md`, `docs/guide-screenshot-set.md`, `dist/build-info.json`, `SUMMARY.md`.
- Commands run: inspected package files and existing guide screenshot renderers with `sed`; created `docs/blog-components/svelte`; added Svelte components and usage docs; updated guide image insert docs and screenshot set docs; `node --check daytona-forking-coupon-demo/docs/blog-components/svelte/index.js`; `rg` checks for `MultiLevelForkTree`, `LifecycleStateSemantics`, and guide insert references; `rg --files daytona-forking-coupon-demo/docs/blog-components/svelte`; `npm run build`.
- Test results: the component export file passed `node --check`. The existing demo build still passes and regenerated `dist/client`. Documentation references point to the new Svelte components and retain PNG fallbacks.
- Screenshots captured: none. These are source components for the blog, not regenerated screenshot assets.
- Remaining TODOs: copy `docs/blog-components/svelte/*.svelte` into the blog's SvelteKit/MDsveX component directory and run that project's Svelte compiler or `svelte-check`.
- Assumptions/blockers: this repo is not a Svelte project and does not include Svelte tooling, so I could not compile the `.svelte` files locally without adding a new dependency/toolchain. The components are intentionally self-contained and use standard Svelte `export let` props for portability.

## Milestone 58: Replaced Svelte blog components with Markdown inserts

- What changed: replaced the Svelte blog component approach with Markdown-only partials for the two diagram-heavy guide sections. The new Markdown inserts use native headings, code blocks, and tables so they can be pasted into a Markdown-only Daytona blog while keeping the fork tree and lifecycle semantics readable. Removed the Svelte component files and updated guide helper docs, screenshot set docs, and the screenshot manifest to recommend the Markdown inserts first with PNGs as fallbacks.
- Files created/modified: `docs/blog-components/markdown/04-multilevel-fork-tree.md`, `docs/blog-components/markdown/05-state-semantics.md`, `docs/blog-components/markdown/README.md`, `docs/guide-image-inserts.md`, `docs/guide-screenshot-set.md`, `docs/screenshots/manifest.md`, `dist/build-info.json`, `SUMMARY.md`. Deleted: `docs/blog-components/svelte/MultiLevelForkTree.svelte`, `docs/blog-components/svelte/LifecycleStateSemantics.svelte`, `docs/blog-components/svelte/index.js`, `docs/blog-components/svelte/README.md`.
- Commands run: inspected existing guide docs and blog component files with `sed`, `rg`, and `find`; deleted Svelte component files; added Markdown partials and a Markdown component README; updated guide docs and screenshot manifest; `npm run build`; searched docs for stale `blog-components/svelte`, `MultiLevelForkTree`, `LifecycleStateSemantics`, and `svelte` references; listed `docs/blog-components` files.
- Test results: `npm run build` passed and regenerated `dist/client`. Stale-reference scans found no remaining Svelte component references under `docs/`. Markdown component paths are referenced from `docs/guide-image-inserts.md`, `docs/guide-screenshot-set.md`, and `docs/screenshots/manifest.md`.
- Screenshots captured: none. These are Markdown replacements for screenshots 4 and 5, not new image assets.
- Remaining TODOs: paste `docs/blog-components/markdown/04-multilevel-fork-tree.md` and `docs/blog-components/markdown/05-state-semantics.md` into the actual guide draft where screenshots 4 and 5 were going to appear.
- Assumptions/blockers: I assumed the blog supports standard GitHub-style Markdown tables and fenced code blocks. If Daytona's blog renderer has narrower table support, the same content can be flattened into bullet lists.

## Milestone 59: Hid generated screenshots/logs and shortened READMEs

- What changed: moved generated screenshots and logs out of visible project folders into `.artifacts/`. Added hidden-artifact path routing so existing scripts that call `projectPath("logs", ...)` or `projectPath("docs", "screenshots", ...)` now read/write `.artifacts/logs/` and `.artifacts/screenshots/`. Regenerated guide screenshot HTML pages in the hidden folder and updated graph image references. Replaced the long root and demo READMEs with concise run/record/artifact instructions.
- Files created/modified: `README.md`, `../README.md`, `scripts/state-utils.js`, `scripts/render-guide-assets.js`, `scripts/capture-screenshots.js`, `scripts/daytona-live-demo.js`, `scripts/simple-demo.js`, `scripts/openai-cache-probe.js`, `scripts/video-latency-report.js`, `scripts/video-terminal-demo.js`, `daytona-examples/snapshot-vs-fork.ts`, `DEMO.md`, `docs/guide-image-inserts.md`, `docs/guide-screenshot-set.md`, `docs/guide-chatgpt-brief.md`, `docs/screenshot-checklist.md`, `docs/blog-components/markdown/README.md`, `data/forking-console-state.json`, `state-manifest.json`, `.artifacts/logs/*`, `.artifacts/screenshots/*`, `dist/build-info.json`, `SUMMARY.md`.
- Files moved: `logs/` moved to `.artifacts/logs/`; `docs/screenshots/` moved to `.artifacts/screenshots/`.
- Commands run: inspected artifact references with `find`, `rg`, and `sed`; patched path routing and user-facing path strings; moved the generated directories with `mv`; bulk-updated docs/data references from `logs/` and `docs/screenshots/` to `.artifacts/logs/` and `.artifacts/screenshots/`; regenerated guide screenshot HTML with `node scripts/render-guide-assets.js`; checked PNG files with `file`; ran `node --check` for `state-utils.js`, `render-guide-assets.js`, `capture-screenshots.js`, `daytona-live-demo.js`, `openai-cache-probe.js`, and `simple-demo.js`; ran `npm run build`, `npm run inspect-state`, and `npm run demo:state`; scanned for visible `logs` and `screenshots` directories outside `.artifacts`.
- Test results: script syntax checks passed. `npm run build` passed and regenerated `dist/client`. `npm run inspect-state` passed and still finds the failing baseline log through the hidden artifact path. `npm run demo:state` passed and now prints `.artifacts/logs` in the parent-inspection example. No visible `logs/` or `docs/screenshots/` directories remain outside `.artifacts`.
- Screenshots captured: none. Existing screenshot files were moved to `.artifacts/screenshots/`; no new PNG captures were needed.
- Remaining TODOs: if the guide draft has already copied image paths, update them from `docs/screenshots/...` to `.artifacts/screenshots/...` or use the Markdown inserts in `docs/blog-components/markdown/`.
- Assumptions/blockers: I kept source diagrams in `docs/graphs/` because those are maintained guide assets, not generated screenshot output. The live Daytona prepared sandbox still describes "failing logs" as a state type, but its generated log files now use `.artifacts/logs/` paths too.

## Milestone 60: Prepared project for private GitHub upload

- What changed: initialized the workspace as a local Git repository and tightened ignore rules so local credential files, OS metadata, dependencies, Python caches, and Daytona home state are not staged. Confirmed the generated guide artifacts in `.artifacts/` remain visible to Git because they are part of the demo/guide package.
- Files created/modified: `.gitignore`, `daytona-forking-coupon-demo/.gitignore`, `daytona-forking-coupon-demo/SUMMARY.md`, local `.git/` metadata.
- Commands run: `git status --short`; `git remote -v`; `rg --files -g '.gitignore' -g '.env*' -g '!node_modules'`; redacted secret scans with `rg`; `gh auth status`; inspected root and demo `.gitignore`; listed project files and artifact files with `find`; checked project size with `du`; patched ignore files; attempted sandboxed `git init`, then reran `git init` with elevated filesystem permission; `git check-ignore -v` for `.env`, demo env files, `node_modules`, and `.DS_Store`; inspected candidate untracked files with `git status --short --untracked-files=all`.
- Test results: `git init` succeeded after approval. `git check-ignore` confirms `.env`, `daytona-forking-coupon-demo/.env`, `daytona-forking-coupon-demo/.env.daytona.local`, `daytona-forking-coupon-demo/node_modules`, and `.DS_Store` are ignored. The refined redacted secret scan found no credential-shaped values in non-ignored candidate files.
- Screenshots captured: none.
- Remaining TODOs: create the private GitHub repository, commit the prepared tree, and push after GitHub CLI authentication is refreshed.
- Assumptions/blockers: `gh auth status` reports the saved GitHub token for `hanabenko` is invalid, so creating the private repository requires re-authenticating `gh` before upload.
